# AI 鞋类商品推荐系统 — 系统文档

## 目录

1. 引言
   - 1.1 编写目的
   - 1.2 背景
   - 1.3 定义
   - 1.4 参考资料
2. 项目说明
3. 项目需求
   - 3.1 功能需求
   - 3.2 非功能需求
   - 3.3 运行环境
4. 概要设计
   - 4.1 系统架构
   - 4.2 模块划分
   - 4.3 数据库设计
   - 4.4 接口设计概要
5. 详细设计
   - 5.1 Agent 决策引擎
   - 5.2 语义检索与知识库 RAG
   - 5.3 流式通信链路
   - 5.4 对话记忆模块
   - 5.5 可观测性追踪
   - 5.6 用户画像模块
6. 编码与实现
   - 6.1 文件清单
   - 6.2 关键代码说明
   - 6.3 部署方式
7. 测试说明
   - 7.1 测试策略
   - 7.2 评测数据集设计
   - 7.3 评测结果
   - 7.4 已知问题
8. 体会与总结

---

## 1. 引言

### 1.1 编写目的

本文档对"AI 鞋类商品推荐系统"进行完整的系统说明，涵盖需求分析、架构设计、模块实现、测试评测等环节。文档面向以下读者：

- 课程验收老师：了解项目整体设计与实现
- 后续开发者：快速理解系统结构与接口
- 面试官：了解技术选型与工程实践

### 1.2 背景

传统的电商推荐依靠关键词搜索和多级筛选，用户需要自己把"膝盖不舒服想找一双适合跑步的鞋"翻译成品类、价位、品牌等结构化条件，门槛较高。近年来大语言模型在自然语言理解和工具调用方面进展显著，但大多数 AI 聊天应用仍停留在简单问答，缺乏与真实商品数据库交互的能力。

本项目将 LangChain ReAct Agent 应用于鞋类推荐场景，让用户直接用自然语言描述需求，Agent 自主决定调用哪些工具、用什么参数，从真实数据库中检索商品并给出有依据的推荐结果。

### 1.3 定义

- ReAct Agent：一种基于"推理—行动—观察"循环的 AI Agent 模式，LLM 在每一步自主决定做什么
- RAG（Retrieval-Augmented Generation）：检索增强生成，先检索外部知识，再让 LLM 结合知识生成回答
- SSE（Server-Sent Events）：服务端推送技术，用于实现流式逐字输出
- Embedding：文本向量化表示，将自然语言转为高维向量，用于语义相似度计算
- Agentic RAG：Agent 主动调用知识检索工具的 RAG 模式，区别于被动检索

### 1.4 参考资料

- LangChain 官方文档：https://python.langchain.com/
- LangGraph 官方文档：https://langchain-ai.github.io/langgraph/
- DeepSeek API 文档：https://platform.deepseek.com/api-docs
- Sentence-Transformers：https://www.sbert.net/
- Spring Boot 4.0 参考文档：https://docs.spring.io/spring-boot/
- Vue 3 官方文档：https://cn.vuejs.org/

---

## 2. 项目说明

本项目是一个全栈 Web 应用，用户可通过浏览器注册登录后，以自然语言对话的方式获得鞋类商品推荐。核心功能包括：

- 智能推荐：用户描述需求（如"扁平足想找缓震跑鞋，预算 500"），Agent 自动调用工具完成搜索、知识查询、推荐
- 多轮对话：Agent 能结合上下文记忆主动追问，逐步缩小需求范围
- 穿搭分析：用户描述穿搭（上装/下装/场合/风格），Agent 匹配鞋款
- 商品对比：对推荐列表中的两款鞋做对比分析
- 专业知识库：覆盖材质、足型、运动场景、品牌定位、尺码、保养 6 个领域
- 对话管理：支持新建、切换、删除对话，刷新或重启不丢失
- 可观测性看板：追踪每次 Agent 调用的延迟、Token、工具链路
- 用户画像：根据收藏行为生成偏好分析

技术栈：Spring Boot（Java 业务后端）+ FastAPI（Python AI 服务）+ Vue 3 CDN（前端）+ MySQL（业务数据）+ SQLite（追踪数据）+ Docker Compose（部署）。

---

## 3. 项目需求

### 3.1 功能需求

系统共有 16 项功能需求：

- F1，AI 对话推荐：用户输入自然语言需求，Agent 返回推荐商品列表（含评分与理由）
- F2，专业知识检索：用户询问材质、足型、保养等问题，Agent 检索知识库后回答
- F3，穿搭分析：用户描述穿搭，Agent 推荐搭配鞋款
- F4，商品对比：对两双鞋做优缺点对比
- F5，主动追问：需求不够明确时，Agent 追问预算、场景、偏好
- F6，流式输出：逐字显示回复内容，同时推送工具调用状态提示
- F7，多轮对话记忆：对话上下文保存，支持刷新页面后恢复
- F8，多对话管理：新建、切换、删除对话记录
- F9，筛选浏览：按性别、品类、品牌、价格区间筛选商品
- F10，用户注册登录：BCrypt 密码加密，Session 会话管理
- F11，收藏管理：添加/取消收藏，个人中心查看收藏列表
- F12，用户画像：基于收藏行为生成偏好概要（品牌、品类、价位、风格标签）
- F13，推荐反馈：对 AI 推荐结果点赞或踩
- F14，管理员 CRUD：管理员可新增、编辑、删除商品
- F15，可观测性看板：统计 Agent 请求数、延迟分位数、Token 趋势、工具分布
- F16，部署支持：Docker Compose 一键启动全部服务

### 3.2 非功能需求

系统共有 6 项非功能需求：

- NF1，响应时间：首 Token 延迟不超过 15 秒（含 LLM 推理），流式输出不中断
- NF2，推荐质量：Agent 评测任务成功率不低于 80%，平均推理步数不超过 5 步
- NF3，可用性：Python 服务异常时自动降级至非流式，前端显示离线提示
- NF4，数据安全：密码 BCrypt 存储，对话记录按 userId 隔离
- NF5，可维护性：模块化文件组织，关键函数有中文注释说明设计意图
- NF6，可观测性：每次 Agent 调用记录 trace_id、耗时、工具链、Token 消耗

### 3.3 运行环境

- 服务器 OS：Ubuntu 24.04 LTS 或 Windows 11
- JDK：17 及以上
- Python：3.11 及以上
- MySQL：8.0
- 内存：不低于 2GB（含 embedding 模型约 400MB）
- 磁盘：不低于 2GB（含模型文件）

---

## 4. 概要设计

### 4.1 系统架构

```
浏览器 (Vue 3 + SSE 流式消费)
       │ HTTP / SSE
Java Spring Boot (端口 8080)
  ├─ Controller 层：REST API + SSE 透传
  ├─ Service 层：业务逻辑
  ├─ DAO 层：JdbcTemplate 直连 MySQL
  ├─ AiAgentClient：调 Python Agent
  └─ static/：前端静态页面
       │ HTTP (内网)
Python FastAPI (端口 5000)
  ├─ LangGraph ReAct Agent
  ├─ 5 个工具函数（闭包注入）
  ├─ ShoeRetriever：语义检索
  ├─ KnowledgeBase：知识库 RAG
  └─ TraceContext：链路追踪
       │ HTTP
DeepSeek API (deepseek-chat)
```

采用双服务架构的原因：Java/Spring Boot 做 Web CRUD 成熟、类型安全；Python 做 AI Agent 生态完整，LangChain 和 SentenceTransformer 都是 Python first。两个服务各用各的生态，内网通信延迟小于 5ms。

### 4.2 模块划分

系统分为 12 个模块：

- 用户模块（Java Controller/DAO）：注册、登录、Session 管理
- 商品模块（Java Controller/DAO）：商品查询、筛选、管理员 CRUD
- 收藏模块（Java Controller/DAO）：收藏增删查
- 反馈模块（Java Controller/DAO）：点赞/踩记录与统计
- 对话历史模块（Java Controller/DAO）：对话列表、详情、删除，对话记忆持久化
- AI 客户端（Java AiAgentClient）：HTTP 调 Python，SSE 透传与降级
- Agent 引擎（Python agent.py）：ReAct 循环，流式/非流式双模式
- 工具包（Python tools.py）：5 个工具定义，闭包注入数据
- 语义检索（Python retriever.py）：SentenceTransformer 向量化 + 余弦相似度 + 缓存
- 知识库（Python knowledge_base.py）：6 领域 MD 分块 + BM25 + 向量混合检索
- 链路追踪（Python trace.py）：SQLite 存储 + TraceContext 上下文
- 前端 UI（static/*.html）：Vue 3 响应式页面

### 4.3 数据库设计

系统共有 5 张表。

#### user（用户表）

- id：主键，自增
- username：唯一，不可重复
- password：BCrypt 加密存储
- role：user 或 admin
- create_time：注册时间

#### shoe_product（商品表）

- id：主键，自增
- name：商品名称
- brand：品牌
- category：品类
- gender：适用性别（male/female/unisex）
- price：价格
- image_url：商品图片 URL
- stock：库存数量
- description：商品描述
- color：颜色
- size_range：尺码范围
- create_time：创建时间

#### favorite（收藏表）

- id：主键，自增
- user_id：外键，关联 user 表
- product_id：外键，关联 shoe_product 表
- create_time：收藏时间
- 约束：UNIQUE(user_id, product_id)，防止重复收藏

#### ai_chat_history（对话记录表）

- id：主键，自增
- user_id：外键，关联 user 表
- conversation_id：对话分组标识，同一对话的多条记录共享
- role：user 或 ai
- content：消息内容
- create_time：发送时间

#### ai_feedback（推荐反馈表）

- id：主键，自增
- user_id：外键，关联 user 表
- conversation_id：关联的对话标识
- user_message：用户当时的提问
- ai_reply：AI 的回复
- feedback：like 或 dislike
- create_time：反馈时间
- 索引：(user_id, conversation_id) 复合索引

表之间的关系：user 一对多 favorite 和 ai_chat_history，shoe_product 一对多 favorite。favorite 是 user 和 shoe_product 的多对多中间表。

### 4.4 接口设计概要

系统共有 20 个 API 端点，分为 5 组：

用户相关接口：

- POST /api/user/register：注册
- POST /api/user/login：登录
- POST /api/user/logout：退出
- GET /api/user/status：获取当前登录用户

收藏与画像接口：

- GET /api/user/favorites：收藏列表（JOIN 商品表返回完整信息）
- POST /api/user/favorite/{id}：添加收藏
- DELETE /api/user/favorite/{id}：取消收藏
- GET /api/user/profile：用户画像数据

对话管理接口：

- GET /api/user/conversations：对话列表
- GET /api/user/conversation/{id}：对话详情
- DELETE /api/user/conversation/{id}：删除对话
- POST /api/user/feedback：提交推荐反馈
- POST /api/user/ai-history：保存对话历史
- GET /api/user/ai-history：查询对话历史

商品接口：

- GET /api/products：获取全部商品
- GET /api/products/{id}：获取单个商品
- GET /api/products/recommend：传统筛选推荐

AI 接口：

- POST /api/ai/chat：AI 对话（非流式）
- POST /api/ai/chat/stream：AI 对话（流式 SSE）
- GET /api/ai/health：AI 服务健康检查

管理员接口：

- POST /api/admin/products：新增商品
- PUT /api/admin/products/{id}：更新商品
- DELETE /api/admin/products/{id}：删除商品

可观测性接口（Python 端）：

- GET /admin/stats：聚合统计数据
- GET /admin/stats/recent：最近 trace 明细

---

## 5. 详细设计

### 5.1 Agent 决策引擎

文件位置：ai-service/agent.py。

Agent 基于 LangGraph 的 create_agent 构建，底层是 langgraph 的有向无环图（DAG），每个节点代表 Agent 的一步决策。

ReAct 循环流程：用户输入后 Agent 进行推理（Thought，决定下一步该做什么），然后执行行动（Action，调用工具），观察结果（Observation，工具返回结果），基于结果再次推理，循环直至得出最终回答。

关键配置：

- 模型：deepseek-chat，temperature=0.3（降低随机性，保证推荐结果稳定）
- 递归上限：recursion_limit=15（防止死循环。实测曾出现过 Agent 连续调用 12 次 search_products 的情况，加限制后不再复现）
- 两种运行模式：process_message() 用于非流式 API 返回完整结果，stream_agent() 用于流式 SSE 通过 astream_events v2 捕获工具调用和 token 事件

系统提示词包含三个部分：

1) 行为规则（6 条）：指导 Agent 何时调哪个工具
2) 防幻觉双检规则：禁止编造（只能使用 search_knowledge 返回的知识）、强制来源标注（格式为"根据【XX领域·XX】"）、置信度分级处理（high 直接引用、medium 保留语气、low 标注未确认）
3) 输出格式要求：推荐列表使用 JSON 格式，回复用口语化中文，禁止 markdown 标题和 emoji

系统提示词（当前线上版本）：

```
你是 AI 鞋类推荐助手。你必须主动使用工具搜索商品。

行为规则：
1. 用户提到鞋类需求 → 立即调 search_products 搜索
2. 用户描述穿搭 → 立即调 analyze_outfit
3. 用户要求对比 → 立即调 compare_shoes
4. 信息不够时 → 调 ask_clarify 追问，问完继续搜
5. 用户问专业知识 → 先调 search_knowledge 获取专业知识，再结合商品推荐
6. 防幻觉双检规则：
   a) 禁止编造：只能使用 search_knowledge 返回的知识内容
   b) 强制来源标注：引用知识格式为"根据【XX领域·XX】"
   c) 置信度分级处理：high 直接引用，medium 保留语气，low 标注未确认
```

### 5.2 语义检索与知识库 RAG

#### 商品语义检索

文件位置：ai-service/retriever.py。

处理流程：商品名称和描述拼接成文本，用 SentenceTransformer 向量化，用户 query 同样向量化，计算余弦相似度排序，返回 top-N 候选结果。

- 模型：shibing624/text2vec-base-chinese，和知识库共用同一个模型，约 400MB
- 向量索引缓存：get_retriever() 用商品 ID 列表的 MD5 hash 做 key，同一批商品只建一次索引，后续请求直接复用
- 未使用 FAISS/Milvus 的原因：当前 353 个商品，numpy 数组内存不到 5MB，全量内积计算不到 1ms，向量数据库的优势在万级以上才体现

#### 知识库 RAG

文件位置：ai-service/knowledge_base.py。

采用三层架构：深度解析、混合检索、生成校验。

Layer 1，深度解析：

- 主块：按 ## 二级标题分块，一个块等于一个独立知识点
- 子块：按 ### 三级标题建立子索引，命中子块时返回完整的父块上下文
- 自动标签：从内容中用正则提取品牌名、技术术语、足型关键词
- 领域标签：保留一级标题作为领域归类

Layer 2，混合检索：

- 向量检索：SentenceTransformer 余弦相似度
- BM25 关键词检索：纯 Python 实现，中文用字级 bigram 分词（"扁平足"拆分为"扁、平、足、扁平、平足"），零外部依赖
- RRF（Reciprocal Rank Fusion）融合：向量权重 0.6 + BM25 权重 0.4，加权合并排序
- 重排序规则：相似度低于 0.35 直接丢弃；同一文件最多 2 个块上榜，超出部分降权 30%

Layer 3，生成校验：

- 每个检索结果附带 confidence 字段（high、medium、low 三级）
- Agent 根据置信度分级引用，低置信度内容标注"暂未完全确认"

知识文件清单（6 个文件，共 40 个主块 + 54 个子块）：

- 01-材质科技.md（8 个主块）：EVA、TPU、碳板、气垫/Air Max、Boost/Lightstrike、Gore-Tex 防水、飞织鞋面、耐磨橡胶外底
- 02-足型选购.md（5 个主块，含 15 个子块）：扁平足、高足弓、宽脚、拇趾外翻、窄脚
- 03-运动场景.md（7 个主块，含 16 个子块）：公路跑步、马拉松竞速、越野跑、篮球、日常通勤、健身房力量训练、登山徒步
- 04-品牌定位.md（7 个主块，含 21 个子块）：Nike、adidas、ASICS、New Balance、Saucony 等
- 05-尺码指南.md（7 个主块）：尺码基础知识、跑步鞋尺码、品牌偏差、试穿时间、两脚不一样大的处理、在线买鞋注意事项
- 06-保养清洁.md（6 个主块）：网面跑鞋清洗、皮革翻毛皮鞋清洗、Gore-Tex 保养、运动鞋除臭、鞋底磨损与换鞋时机、球鞋存放

### 5.3 流式通信链路

完整链路：Python FastAPI 产生 SSE 事件流，Java Spring Boot 透传，浏览器消费渲染。

Python 端（产生流）：

使用 astream_events v2 获取异步事件流，三类事件分别处理：

1) on_tool_start 事件：推送工具名称和中文状态文案给前端（如"正在为你搜索合适的鞋款"）
2) on_chat_model_stream 事件：逐 token 推送，每个 token 立即 yield
3) on_tool_end 事件：推送工具完成信号，清除前端加载状态

FastAPI 的 StreamingResponse 配合 async generator，每个 yield 立即 flush 到 HTTP 响应流。

Java 端（透传流）：

使用原生 HttpURLConnection 而非 RestTemplate。原因是 RestTemplate 默认缓冲完整响应，会导致 Python 端逐 token 推送但 Java 端等到全部收完才转发，流就断在这里。具体做法是用 BufferedReader.readLine() 逐行读取 Python 的 SSE 事件，读到一行立刻 PrintWriter.write() + flush() 到 HttpServletResponse。

关键配置：response.setHeader("X-Accel-Buffering", "no") 禁用代理缓冲，Cache-Control: no-cache 防止浏览器缓存流。

前端（消费流）：

fetch() + response.body.getReader() 读 ReadableStream，逐行解析 SSE 格式。根据事件类型更新 UI：token 事件追加到对话气泡，tool_start/tool_end 事件显示或隐藏工具调用标签，done 事件渲染商品卡片和追问标签。

降级策略：astream_events 抛异常时，catch 后用 agent.invoke() 一次性返回完整结果，前端从打字模式切换到全文模式，不会出现白屏。

### 5.4 对话记忆模块

采用三层架构：

- 第一层，Python 内存（conversations 字典）：进程存活期间 Agent 每次推理直接读，零延迟
- 第二层，MySQL（ai_chat_history 表）：永久存储，重启不丢失
- 第三层，localStorage（shoe_current_conv_{userId}）：浏览器端存储当前对话 ID，刷新页面自动恢复

截断策略（防止上下文膨胀）：

1) Java 端：loadConversationHistory() 从 MySQL 只取最近 20 条（10 轮对话）
2) Python 端：_trim_history() 每轮对话后兜底截断，成对删除 HumanMessage 和 AIMessage（保证不会出现只删了用户消息、留着 AI 消息的错位）
3) 选 10 轮的依据：DeepSeek 128K 上下文远超 10 轮需求，但太远的对话对当前推荐没有帮助，用户不会为买一双鞋聊 100 轮

恢复流程：Python 重启后内存清空，用户打开旧对话时 Java 从 MySQL 查出历史传入，Python 的 _restore_history() 将历史消息灌回内存，Agent 恢复正常工作。

### 5.5 可观测性追踪

存储：SQLite traces.db（agent_traces 表），与 MySQL 业务数据分离。

记录字段：trace_id（追踪标识）、timestamp（时间戳）、duration_ms（总耗时）、first_token_ms（首 Token 延迟）、tool_calls（工具调用 JSON 数组）、tokens_input（输入 Token 估算值）、tokens_output（输出 Token 估算值）、error（错误信息）、conversation_id（关联对话）。

实现方式：TraceContext 类封装每次请求的追踪上下文。start() 开始计时，on_tool_start() 和 on_tool_end() 记录每个工具的耗时，end() 写入 SQLite。写库失败时静默忽略，不影响 Agent 主流程。

Token 估算方法：中文按 1.5 字符/token，英文按 4 字符/token。和非流式 API 返回的精确 token 数对比，误差在正负 15% 以内，趋势分析够用。

前端看板：admin/stats.html 用 Chart.js 绘制 6 组图表——概览卡片（请求数、延迟、Token、错误率、费用）、每小时请求量柱状图、工具分布横向柱状图、每日 Token 趋势折线图、P50/P95/P99 延迟分位数、最近 50 条 Trace 明细表。

### 5.6 用户画像模块

数据来源：收藏记录，JOIN shoe_product 获取完整商品信息。

计算维度：

- 偏好品牌：去重集合，按收藏频次排序
- 偏好品类：同上
- 收藏均价：所有收藏商品价格的平均值
- 风格标签：根据均价判定"高端型、中端型、性价比型"；收藏数达到 5 件标注"收藏达人"；品类覆盖 3 个及以上标注"多元化"；品类只有 1 个标注"专注某品类党"

注入方式：画像以自然语言拼入 System Prompt，格式为"该用户画像：偏好品牌 Nike、Adidas；偏好鞋类 跑鞋；平均收藏价位约 680 元；"，Agent 据此做个性化推荐但不刻意提"画像"二字。

前端展示：首页新增"我的画像"Tab，三列指标卡片（收藏数、对话数、消息数）加四格详情（品牌、品类、均价、反馈）加最近收藏列表。

---

## 6. 编码与实现

### 6.1 文件清单

#### Python AI 服务（ai-service/）

- agent.py（约 330 行）：Agent 核心，含 LLM 客户端、系统提示词、对话记忆、流式/非流式处理、回复解析
- tools.py（约 170 行）：5 个工具定义，含 search_products、analyze_outfit、compare_shoes、ask_clarify、search_knowledge
- retriever.py（约 90 行）：语义检索器，SentenceTransformer 向量化 + 余弦相似度 + 内存缓存
- knowledge_base.py（约 260 行）：知识库，三层 RAG（深度解析 + BM25 + RRF 融合 + 重排序）
- trace.py（约 184 行）：链路追踪，SQLite 存储 + TraceContext 上下文管理器
- main.py（约 218 行）：FastAPI 入口，SSE 流式端点 + 统计 API + CORS 中间件
- schemas.py（约 25 行）：Pydantic 数据模型
- config.py（约 10 行）：环境变量读取
- eval_dataset.py（约 210 行）：30 条标准评测用例
- eval_engine.py（约 340 行）：评测引擎，四指标一键跑 + 影子测试对比
- knowledge/ 目录（约 350 行）：6 个领域知识文件

#### Java 后端（aishoesrecommend/）

- ShoeProductController.java：商品 API + AI 对话入口 + 用户画像接口
- UserController.java：注册、登录、退出（BCrypt + Session）
- FavoriteController.java：收藏 CRUD（JOIN 商品表）
- FeedbackController.java：推荐反馈（like/dislike）
- AiChatHistoryController.java：对话列表、详情、删除
- AdminProductController.java：管理员商品 CRUD
- AiAgentClient.java：调 Python AI 服务（HttpURLConnection SSE 透传）
- 5 个 DAO 类：JdbcTemplate 裸 SQL 数据访问
- 5 个 Entity 类：JavaBean 映射
- 3 个 Service 类：业务逻辑封装

#### 前端（static/）

- index.html：主页，含 AI 对话、商品浏览、用户画像、管理员 CRUD、对话管理
- login.html：登录页
- register.html：注册页
- profile.html：个人中心（收藏列表）
- admin/stats.html：可观测性看板

#### 部署文件

- docker-compose.yml：三服务编排（MySQL + Python + Java）
- ai-service/Dockerfile：Python 镜像（pip install + 预下载模型）
- aishoesrecommend/Dockerfile：Java 多阶段构建（Maven 构建 + JRE 运行）
- docker-init.sql：容器首次启动自动建表并写入种子数据

### 6.2 关键代码说明

#### 工具闭包注入

文件位置：tools.py。

5 个工具函数不依赖全局变量。每次请求调用 create_tools(products, retriever, knowledge_base) 创建新的工具实例，商品数据通过闭包传入。这样保证多用户并发时各自的工具实例不会读到别人的数据。

#### Java SSE 透传

文件位置：AiAgentClient.java。

核心代码：

```java
HttpURLConnection conn = (HttpURLConnection) url.openConnection();
BufferedReader reader = new BufferedReader(
    new InputStreamReader(conn.getInputStream(), StandardCharsets.UTF_8));
String line;
while ((line = reader.readLine()) != null) {
    if (line.startsWith("data: ")) {
        writer.write(line + "\n\n");
        writer.flush();  // 关键：立即 flush，不缓冲
    }
}
```

不使用 RestTemplate 的原因：它的 SimpleClientHttpRequestFactory 默认不设置 bufferRequestBody，底层 InputStream 会被完全读完才返回，导致流式体验丢失。

#### 非流式工具调用记录

文件位置：agent.py。

非流式模式不走 astream_events，需要通过检查 agent.invoke() 返回的消息列表来提取 tool_calls：

```python
for msg in result.get("messages", []):
    if hasattr(msg, "tool_calls") and msg.tool_calls:
        for tc in msg.tool_calls:
            trace_ctx._tool_calls.append({
                "name": tc.get("name", "unknown"),
                "duration_ms": 0,
            })
```

这样保证非流式模式也能在 trace 表中记录工具调用信息。

#### 品牌中英文映射

文件位置：tools.py 中的 _apply_filters 函数。

数据库品牌字段是英文，但用户可能说中文。通过 BRAND_MAP 做映射：

```python
BRAND_MAP = {
    "阿迪达斯": "adidas", "adidas": "adidas",
    "耐克": "nike", "nike": "nike",
    "匡威": "converse", "converse": "converse",
    ...
}
```

映射后用统一的小写英文比较。gender 字段同理（"男"映射为"male"，"女"映射为"female"）。

### 6.3 部署方式

Docker Compose 一键启动：

```bash
cp .env.example .env          # 编辑填入 MySQL 密码和 DeepSeek API Key
docker compose up -d           # 启动 MySQL + Python + Java 三个容器
```

容器编排细节：

1) MySQL 最先启动，带 healthcheck（mysqladmin ping），健康后才允许下游启动
2) Python 依赖 MySQL healthy 后启动，模型预热需要约 60 秒 start period
3) Java 依赖 Python healthy 后启动
4) Java 通过环境变量 AI_SERVICE_URL=http://ai-service:5000 连接 Python，Compose 网络内用容器名做 DNS 解析
5) docker-init.sql 挂载到 MySQL 的 /docker-entrypoint-initdb.d/ 目录，首次启动自动执行建表和种子数据

---

## 7. 测试说明

### 7.1 测试策略

本项目的测试分为三类：

- 功能手动测试：注册、登录、AI 对话、收藏、反馈、对话管理等流程手动走通
- 量化评测：30 条标准用例，Python 脚本一键跑，测量 Agent 的任务成功率、推理步数、工具准确率
- 影子测试：同一条 query，Agent（带工具）和裸 LLM（无工具）分别回答，对比回复质量

### 7.2 评测数据集设计

30 条测试用例覆盖 5 种 Agent 意图：

- 商品搜索（search），11 条用例：覆盖品牌名称、品类词、价位区间、性别偏好、材质描述等场景
- 知识检索（knowledge），6 条用例：覆盖足型选鞋、材质对比、碳板安全性、防水透气、清洗保养等场景
- 穿搭分析（outfit），4 条用例：覆盖场合（约会、通勤、逛街）、季节和风格偏好
- 商品对比（compare），4 条用例：覆盖两两对比、品牌对比、优缺点分析等场景
- 模糊需求追问（clarify），5 条用例：覆盖信息极少的开放问题（如"推荐一双鞋"、"我想买鞋"）

每条用例包含以下字段：

- query：用户输入的自然语言
- intent：意图类型
- expected_tools：期望 Agent 调用的工具（至少命中一个即算工具选择正确）
- min_results：期望最少返回几个商品（0 表示不要求）
- key_terms：回复中应出现的关键术语（用于验证 Agent 是否使用了正确知识）

评测引擎为 eval_engine.py，核心流程是逐条跑用例，对比期望值和实际值，判定成功或失败，输出报告并存入 eval_runs 表。

### 7.3 评测结果

评测日期：2026-06-24。

总览数据：

- 总用例数：30 条
- 任务成功率：90.0%（27 条成功，3 条失败）
- 工具调用准确率：86.7%（26 条命中期望工具）
- 平均推理步数：2.5 步每轮

按意图分类数据：

- 知识检索：成功率 100%（6/6），平均 3.7 步。知识问题 Agent 会先查知识库再结合商品推荐，步数多 1-2 步是"知识联动商品"的正常开销
- 穿搭分析：成功率 100%（4/4），平均 2.8 步。analyze_outfit 工具稳定触发
- 商品对比：成功率 100%（4/4），平均 3.0 步。通常先查商品再对比
- 商品搜索：成功率 82%（9/11），平均 2.1 步。最常见的意图，Agent 非常稳定
- 模糊追问：成功率 80%（4/5），平均 1.2 步。Agent 信息不够时主动追问，1-2 步就停

三个意图达到 100% 成功率，说明 Agent 对明确意图的处理很稳定。

工具调用分布（30 条共产生 74 次工具调用）：

- search_products：35 次，占 47%
- search_knowledge：25 次，占 34%
- ask_clarify：9 次，占 12%
- analyze_outfit：4 次，占 5%
- compare_shoes：1 次，占 1%

search_products 和 search_knowledge 合计占 81%，符合"搜索加知识"的核心定位。ask_clarify 调用 9 次说明 Agent 在信息不足时会主动追问而不是硬搜。

影子测试结果：Agent 在 57%（17/30）的查询中返回了结构化商品卡片（含 ID、名称、价格、评分），裸 LLM 完全做不到。这是 Agent 不可替代的核心价值——不是聊天的玩具，是能和真实数据库交互的推荐系统。

失败用例分析：

- S01、S04：DeepSeek API 偶发超时，返回"AI 暂不可用"，不是 Agent 逻辑问题
- Q02："我想买鞋"极度模糊需求，Agent 调了 search_products 但评测标准判定工具选择不对。实际上 Agent 的回复已主动追问预算和场景，交互质量本身合格

### 7.4 已知问题

1) Agent 在部分搜索场景会出现"搜到了但说搜不到"的不一致，system prompt 引导逻辑与实际行为偶有偏差
2) 商品图片 98% 为占位卡片，影响演示视觉体验
3) 评测依赖 DeepSeek API 可用性，偶发超时会拉低成功率
4) 穿搭分析和商品对比场景的测试覆盖偏少（合起来只占 8 条），且实际触发频率依赖对话上下文
5) 向量索引缓存基于商品 ID 列表 hash，管理员增删商品后不会自动刷新

---

## 8. 体会与总结

这个项目最开始只是一个 Spring Boot CRUD 加一个调 DeepSeek API 的聊天界面。在迭代过程中，每解决一个问题就引出下一个：加了语义检索后发现纯向量匹配可能召回品类不相关的商品，于是加了结构化过滤层；加了知识库后发现纯向量检索对"扁平足"这种查询没问题，但对"Nike 气垫"这种品牌加技术混合查询效果不好，于是加了 BM25 混合检索；发现 Agent 偶尔会陷入死循环连续搜索 12 次，于是加了 recursion_limit 和空结果提示信号。

最大的体会是，做 AI 应用不是接一个大模型 API 就完事了。真正花时间的是四个东西：

1) 工具体系怎么设计：让 Agent 知道何时调哪个工具，工具描述怎么写 LLM 才不会乱调
2) 检索怎么才准：语义检索、关键词检索、结构化过滤三层配合，单靠任何一层都不够
3) 出错怎么兜底：降级策略和防护机制比正常流程更重要
4) 效果怎么衡量：量化评测而不是凭感觉说"还行"

技术上做得比较好的地方：

1) Agent 的工具编排是真 ReAct，没有用 if-else 替代决策。评测数据证明 Agent 在 5 种意图下都能选对工具
2) SSE 流式链路三跳全通。Java 用原生 HttpURLConnection 透传这个方案是踩了 RestTemplate 缓冲的坑后试出来的
3) 可观测性不是事后补的。trace 埋点、SQLite 存储、Chart.js 看板，改 prompt 后能知道 token 涨了没、工具调用变了没
4) 评测体系不是事后写的。30 条标准用例在代码写完的同时就有了，以后每次改都能跑一遍

下一步可以改进的方向：

1) 评测体系加入 LLM-as-Judge（让另一个 LLM 评判推荐结果是否符合用户意图），替代纯关键词匹配的判定方式
2) 引入推荐收藏转化率等业务指标，从"系统跑得通"走到"推荐做得好"
3) 向量索引迁移到 FAISS 或 Milvus（商品超过 1000 条时需要）
4) 穿搭分析和商品对比两个工具的实际触发率偏低，system prompt 可以增加触发引导
5) 加入摘要记忆（前 10 轮之前的对话压缩为一句偏好摘要），解决超长对话的记忆丢失问题

这个项目让我完整地走了一遍 AI 应用从 0 到 1 的过程——需求分析、架构设计、代码实现、评测验证。不仅是写代码，更重要的是学会了怎么判断自己做得好不好。
