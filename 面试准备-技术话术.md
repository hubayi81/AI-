# AI 鞋类推荐助手 — 面试拷打应答

> 适用岗位：AI 应用开发 / 后端开发 / 全栈开发 实习或校招
> 回答策略：先说结论，再说细节，最后补 trade-off。展现你知道的不只是"怎么做"，更是"为什么这样做"。

---

## 一、开场：一句话抓住注意力

面试官："介绍一下这个项目。"

> 我做了一个 AI 鞋类导购助手。用户用自然语言描述需求——比如"我膝盖不好，想找缓震好的跑鞋，预算 800 以内"——系统能像专业导购一样理解需求、调用工具搜索商品、结合专业知识给出有依据的推荐。
>
> 技术栈：Spring Boot + FastAPI 双服务，核心是基于 LangChain ReAct Agent 的自主工具编排，SentenceTransformer 做语义检索代替传统关键词搜索，SSE 实现流式输出让用户不用干等，最近加了一套鞋类专业知识库做 Agentic RAG 增强推荐质量。部署用 Docker Compose。

**必须出现的三个关键词**：ReAct Agent、语义检索、流式输出。如果面 AI 应用岗，再加"Agentic RAG"。

---

## 二、面试官拷打路线图

面试官一般按这个逻辑层层递进。每个方向 2-3 道追问，答好了就换方向，答不好就深挖。

```
自我介绍
  │
  ├── 拷打方向 1：架构决策
  │   "为什么是两个服务？"（Q1）
  │     └─ "那为什么不用微服务框架？"（Q2）
  │
  ├── 拷打方向 2：AI 核心
  │   "Agent 做了什么？"（Q3）
  │     └─ "和直接调 API 有什么本质区别？"（Q4）
  │     └─ "LangChain create_agent 和旧版的区别？"（Q5）
  │
  ├── 拷打方向 3：检索系统
  │   "语义检索怎么做的？"（Q6）
  │     └─ "为什么不用 Elasticsearch？"（Q7）
  │
  ├── 拷打方向 4：流式链路
  │   "SSE 怎么实现的？"（Q8）
  │     └─ "为什么 SSE 不用 WebSocket？"（Q9）
  │     └─ "Java 透传遇到什么坑？"（Q10）
  │
  ├── 拷打方向 5：个性化
  │   "用户画像怎么做的？"（Q11）
  │     └─ "为什么不用协同过滤？"（Q12）
  │
  ├── 拷打方向 6：脆弱环节
  │   "DeepSeek 挂了怎么办？"（Q13）
  │   "并发大了有什么瓶颈？"（Q14）
  │   "token 成本怎么控？"（Q15）
  │
  ├── 拷打方向 7：工程判断
  │   "前端为什么这么选？"（Q16）
  │   "这个项目最大的不足？"（Q17）
  │   "如果重新做，会改什么？"（Q18）
  │
  └── 拷打方向 8：RAG 专项（单独文档）
      见《面试准备-RAG知识库拷打.md》
```

---

## 三、逐题应答

---

### Q1: 为什么是 Spring Boot + FastAPI 两个服务？一个服务不行吗？

**面试官在考**：你是不是为了简历好看而过度设计了。

> 两个服务不是因为"微服务更高级"，而是两个语言生态各有所长，强行合在一起反而更复杂。
>
> Spring Boot 负责用户系统——登录、session、收藏、历史记录、反馈。这些是标准的三层 CRUD，Java 的类型安全 + JdbcTemplate 直接写 SQL，我可控性最好。
>
> Python FastAPI 负责 AI——LangChain 和 SentenceTransformer 的生态全在 Python。如果强行用 Java 调 Python ML 库，要么走 JNI（噩梦级调试），要么启动 Python 子进程（每次都冷启动模型，慢到不能用）。
>
> 替代方案：全用 FastAPI 写业务——但我当时对 Python 的 ORM 不熟，直接用 JDBC 写 SQL 更顺手。全用 Spring AI——但 Spring AI 对 DeepSeek 的支持和中文语义检索当时不够成熟。
>
> 代价：多了一跳 HTTP 调用（内网 <5ms，可以忽略）。好处：各自独立部署和扩容。以后如果想给 AI 服务上 GPU 机器，改个地址就行，用户系统不受影响。

---

### Q2: 那为什么不加注册中心、网关、熔断？这就是个"伪微服务"吧？

**面试官在考**：你能不能区分"服务拆分"和"微服务架构"，有没有被框架绑架。

> 这是两个服务独立部署，不是微服务架构——我没有用 Spring Cloud、Nacos、Sentinel。原因很简单：不需要。
>
> 微服务那套基础设施（注册中心、网关、熔断、链路追踪）解决的问题是"几十个服务之间的调用关系复杂到人脑理不清"。我这里就两个服务，一个调另一个，拓扑简单到一眼看完。
>
> 引入 Spring Cloud 的收益为零，但成本巨大——配置 Nacos 的时间比我写业务逻辑还长。大二学生项目不应该追求架构完备性，应该追求"刚好够解决问题"。
>
> 但我不能说没想过这些。如果以后服务拆多了——比如把知识库单独抽成一个服务、加一个评测服务——我会先引入一个轻量网关（Nginx 或 Traefik）做反向代理和限流，再考虑熔断。演进顺序应该是：先发现问题（比如 Python 服务挂了导致 Java 线程卡死），再引入对应解决方案。而不是反过来——先把全套微服务基础设施搭好，再往里填业务代码。

---

### Q3: Agent 做了什么？你给了它什么工具？

**面试官在考**：你理解 Agent 的本质——不是调 API，是让 LLM 自主编排工具。

> Agent 收到用户消息后，自主决定"下一步做什么"：搜商品、查知识、对比、追问、还是直接回复。这个决策循环（思考→行动→观察→再思考）由 LangChain 的 create_agent 实现，我只需要定义工具有哪些——LLM 自己决定什么时候用、按什么顺序用、用了之后要不要换个角度再查。
>
> 给 Agent 的工具：
> - `search_products`：语义搜索鞋款，支持类目/品牌/性别/价格过滤
> - `analyze_outfit`：根据穿搭描述匹配鞋款（上装/下装/场合/风格 → 鞋）
> - `compare_shoes`：对比两双鞋的优劣
> - `ask_clarify`：信息不够时主动追问，而不是瞎猜
>
> 加上 RAG 模块后还有 `search_knowledge`：查鞋类专业知识（材质、足型、运动场景）。
>
> 关键区别：如果直接调 API 做 function calling，我需要自己写循环逻辑——调了工具 → 拿到结果 → 判断是否继续调 → 拼接上下文 → 再调 LLM。这不只是多写代码的问题，而是边界 case 太多：工具返回空了怎么办？Agent 陷入死循环了怎么办？中间结果怎么管理？LangChain 帮我封装了这些，省下的精力可以花在提升推荐质量上。

---

### Q4: 那和直接调 DeepSeek API 的 function calling 有本质区别吗？你不就是套了个 LangChain 壳？

**面试官在考**：你有没有真正理解 LangChain 帮你做了什么，还是只是"用了就用了"。

> 本质区别在于三个能力：
>
> 1. 流式中间状态：用 `astream_events v2`，我能捕获到"工具开始执行""LLM 逐 token 产出""工具执行结束"三类事件。原生 function calling 只能拿到最终结果，中间过程对我是黑盒。前端显示"正在为你搜索合适的鞋款..."——这个体验就是靠捕获中间事件实现的。
>
> 2. 错误恢复和重试：如果工具调用返回了格式错误的结果，Agent 能自己重试——"这次搜到的结果太少，换个关键词再搜"。原生 API 需要你在客户端写重试逻辑。
>
> 3. 多工具编排：用户说"我扁平足，帮我找一双跑鞋，和我的 UltraBoost 对比一下"——Agent 需要先查知识库（扁平足选鞋标准），再搜商品，再找出 UltraBoost 在不在库里做对比。这个链条的编排逻辑，用原生 function calling 需要我写一套状态机。
>
> 说它是"套壳"也对——LangChain 确实封装了底层 API 调用。但封装的价值不在于"少写代码"，而在于让我在更高的抽象层工作：不关心"怎么调 API"，关心"Agent 的工具体系怎么设计、工具描述怎么写 LLM 才不会乱调"。

---

### Q5: LangChain create_agent 和旧版 AgentExecutor 有什么区别？

**面试官在考**：你跟进了技术更新，还是随手用了教程里的代码。

> `create_agent` 是 LangChain 0.3 推出的新 API，底层基于 langgraph 的状态图。相比旧版 `AgentExecutor`：
>
> - 流式支持：旧版 AgentExecutor 的中间步骤很难获取——你能看到 Agent 调了工具，但看不到调的是什么、中间状态是什么。`create_agent` + `astream_events v2` 能捕获 `on_tool_start`/`on_tool_end`/`on_chat_model_stream`，我才能给前端展示"正在搜索…"。
> - API 简洁度：旧版需要手动配 AgentExecutor(memory=..., max_iterations=..., handle_parsing_errors=...)。新版 `create_agent(llm, tools, system_prompt=...)` 三个参数。
> - 底层图结构：langgraph 本质是一个有向无环图，Agent 的每一步是图的一个节点。这为以后自定义复杂的 Agent 流程（比如"先查知识 → 后搜商品 → 再对比"的固定 Pipeline + LLM 自主决策的混合模式）留下了扩展空间。

---

### Q6: 语义检索怎么做的？为什么不用 MySQL LIKE 模糊查询？

**面试官在考**：你理解"语义匹配"和"关键词匹配"的本质区别，以及选型时的量级意识。

> 用 SentenceTransformer 的 `text2vec-base-chinese` 模型。流程：启动时把所有商品描述向量化（name + description 拼接），存到 numpy 数组。用户 query 到来时同样向量化，用余弦相似度计算 top-k 返回最相关的商品。
>
> 为什么不用 LIKE？一个实际例子：用户说"膝盖不好想找缓震跑鞋"。商品描述里写的是"减震""缓震""吸震""冲击吸收"——LIKE 匹配"缓震"可能会漏掉所有写"减震"的商品，匹配"膝盖"可能一条都找不到。语义检索能发现"膝盖不好"和"减震跑鞋"在语义上是强相关的——即使字面上完全不同。
>
> 为什么不用 MySQL 全文索引（FULLTEXT）？同样是词匹配问题——"膝盖不好"在全文索引里会被拆成"膝盖""不好"两个词，搜出来的可能是任何包含"膝盖"或"不好"的内容，完全没有语义理解。

---

### Q7: 那为什么不用 Elasticsearch？

**面试官在考**：你是不是会盲目追求"更好的技术"而忽视运维成本。

> ES 确实能做向量检索（8.x 版本后原生支持 KNN），但我没选它的原因：
>
> 1. 数据量：不到 1000 个商品，numpy 数组的内存占用不到 3MB，一次余弦相似度计算不到 1ms。ES 的优势在百万级以上——分布式索引、近似 KNN（ANN）大幅减少计算量。1000 条数据上 ES 的收益为零。
> 2. 运维成本：ES 需要额外的 Java 进程、内存管理、索引维护。Docker 里多跑一个 ES 容器，内存多占 1-2GB。我的阿里云学生机总共就 2GB 内存。
> 3. 部署复杂度：我的 Docker Compose 现在只有 MySQL + Spring Boot + Python 三个服务。加 ES 意味着多一个容器、多一套健康检查、多一套日志。引入 ES 的成本远大于几百行 numpy 代码。
>
> 但我清楚什么时候该切 ES：商品量上万、检索延迟提到 >50ms、需要复杂过滤+排序+聚合时。技术选型的核心不是选"最好的"，而是选"当前规模下刚好够用的，并且有明确的升级路径"。

---

### Q8: SSE 流式输出怎么实现的？完整的链路是什么？

**面试官在考**：你理解端到端的流式链路，而不是只知道"用了 SSE"。

> 链路：Python FastAPI → Java Spring Boot → 浏览器。
>
> Python 端（产生流）：
> - 用 `streaming=True` 的 ChatOpenAI + `create_agent`
> - 调 `agent.astream_events(messages, version="v2")` 获取异步事件流
> - 事件类型有三种：`on_tool_start`（推送工具状态给前端）、`on_chat_model_stream`（逐 token 推）、`on_tool_end`（清状态）
> - FastAPI 的 `StreamingResponse` 配合 async generator，每个 yield 立即 flush
>
> Java 端（透传流）：
> - 这是整个链路最容易踩坑的地方。RestTemplate 会等 HTTP 响应完整接收后才返回——这意味着 Python 已经推了 10 个 token，Java 还在等响应结束。流就断在这里了。
> - 我用原生 `HttpURLConnection` 打开连接，`BufferedReader.readLine()` 逐行读取 Python 的 SSE，读到一行就 `PrintWriter.write()` + `flush()` 到 `HttpServletResponse`。
> - 关键配置：`response.setHeader("X-Accel-Buffering", "no")` 禁用 nginx 缓冲；`Cache-Control: no-cache` 防止浏览器缓存流。
>
> 前端（消费流）：
> - `fetch()` + `response.body.getReader()` 读 `ReadableStream`
> - 每行解析 SSE 格式（`data: {...}`），根据事件类型更新 UI
>
> 降级策略：Python 端 `astream_events` 如果抛异常，catch 后用 `agent.invoke()` 一次性返回，token 字段放完整文本——前端体验会从"逐字打字"变成"一次出全文"，但不会白屏。

---

### Q9: 为什么 SSE 而不用 WebSocket？

**面试官在考**：你理解两种流式技术的适用场景。

> SSE 是单向的（服务端→客户端），WebSocket 是双向的（两端都能主动发消息）。
>
> 我的导购场景：用户发一条消息 → 服务端推回一个流 → 结束。这是典型的单向推送，SSE 完全够用。
>
> SSE 的优势：
> - 基于 HTTP 协议，不需要升级握手。浏览器原生支持 `EventSource`（虽然我用 fetch 手动解析是为了更好的控制）
> - 自动重连——浏览器 `EventSource` 断线会自动重连，WebSocket 需要自己写重连逻辑
> - 穿透代理更友好——HTTP 流大部分代理默认放行，WebSocket 协议升级可能被企业代理拦截
>
> WebSocket 适合的场景：聊天室（双方都主动发消息）、协作编辑（双方都在打字）、实时游戏（双向高频通信）。我的项目是"用户→提问，AI→回复"，单向的。用 WebSocket 多了一个不必要的双向通道。
>
> 如果以后加一个"AI 主动推送"的功能（比如"您收藏的鞋降价了"），那时 WebSocket 或 Server Push 才值得考虑。

---

### Q10: Java 透传 SSE 遇到了什么具体的坑？

**面试官在考**：你有没有实际调试过，还是只是"看起来能跑"。

> 最大的坑就是 RestTemplate 会缓冲完整响应。我第一次写的时候，Java 端用的 `RestTemplate.execute()` 接收 Python 的流——结果前端等了 20 秒然后一次性弹出完整回复。排查了半天发现 RestTemplate 的 `SimpleClientHttpRequestFactory` 默认不设置 `bufferRequestBody`，底层 `InputStream` 会被完全读完才返回。
>
> 第二个坑：`BufferedReader.readLine()` 按行读，但 Python 端的 `yield` 产出的 token 可能没有换行。我需要确保 Python 端每个 SSE 事件后面都有 `\n\n`（双换行是 SSE 协议的标准分隔符），否则 Java 的 `readLine()` 会一直阻塞。
>
> 第三个坑：中文编码。DeepSeek 返回的 token 包含中文，Java 的 `InputStreamReader` 需要显式指定 UTF-8，否则 Windows 系统默认 GBK 会乱码。Python 端也要在 StreamingResponse 里显式配 UTF-8。
>
> 动手之前我以为"就是一个流式转发，几行代码的事"，实际写了 80 行，踩了三个坑才调通。

---

### Q11: 用户画像是怎么实现的？为什么只统计收藏而不是浏览？

**面试官在考**：你理解信号质量和冷启动问题。

> 用户登录后，Java 端查他的收藏记录，统计偏好品牌、品类、平均价位，拼成自然语言注入 system prompt：
>
> "该用户画像：偏好品牌 Nike、ASICS；偏好鞋类 跑鞋；平均收藏价位约 ¥600；共收藏 8 双鞋。推荐时可以优先考虑这些偏好，但不要刻意提'画像'两个字。"
>
> 为什么只用收藏？收藏 = 用户主动操作，是"我喜欢"的明确信号，质量高。浏览行为（页面停留时间、点击次数）噪音很大——用户可能点开一双鞋是因为好奇、误触、或者只是在对比。在没有大量数据（几十万条行为日志）的情况下，用低质量信号做个性化反而会推荐变差。
>
> 为什么不用协同过滤（Collaborative Filtering）？CF 需要"用户-商品"的交互矩阵足够稠密。我的用户量和商品量都不够——用户 A 收藏了 3 双、用户 B 收藏了 5 双，两人的交集可能是空的，CF 算不出任何东西。LLM + 画像注入在冷启动和数据稀疏的场景下天然优于传统推荐模型——LLM 不需要训练，靠常识就能从"喜欢 Nike、跑鞋、¥600"推断出应该优先推荐 Nike 的中端跑鞋。
>
> 以后如果用户量上来了：协同过滤做召回（从海量商品中快速筛 100 个候选），LLM Agent 做精排（从 100 个中挑 5 个并给出个性化理由）。两者是互补关系，不是替代关系。

---

### Q12: 用户不收藏怎么办？画像就一直是空的？

**面试官在考**：你考虑过冷启动的用户体验。

> 确实——新用户或不喜欢收藏的用户，画像是空的，system prompt 里就没有个性化信息，Agent 做通用推荐。
>
> 这不是 bug，是设计意图：通用推荐的质量已经足够好（LLM 理解自然语言需求 + 语义检索找商品），个性化是锦上添花。
>
> 如果要改进，可以加隐式画像——不依赖用户主动收藏，而是分析对话历史。比如用户连续问了三次"跑鞋""轻便""透气"，Agent 可以推断"这个用户偏好轻量透气跑鞋"。但这需要多轮对话的偏好提取能力，复杂度比收藏画像高不少。
>
> 当前的取舍：优先保证"不登录也能得到好推荐"的基线体验，个性化是可选的增强。强制要求用户先收藏再推荐 —— 那个体验更差。

---

### Q13: DeepSeek API 挂了怎么办？

**面试官在考**：你有没有想过失败场景，还是只写了 happy path。

> 三层降级：
>
> 层一，Python Agent 内部：`astream_events` 抛异常 → catch 后用 `agent.invoke()` 非流式一次性返回。体验降级为"没有打字动画，完整回答一次性出"，但不出错。
>
> 层二，Java 调用 Python：`HttpURLConnection` 连接超时或 HTTP 错误 → catch 后向前端写降级 SSE 事件：`{"done":true, "reply":"AI 暂时不可用，请稍后再试。"}` ——前端能正常结束流，不会无限 loading。
>
> 层三，前端 fetch：网络错误 → catch 后显示 toast 提示"网络异常，请重试"。
>
> 没做的：多模型 fallback。理想情况下 DeepSeek 挂了自动切 OpenAI——但需要管理两套 API Key、处理两个模型的格式差异、做健康检查。目前 DeepSeek 的可用性还不错，且多模型 fallback 的代码复杂度 > 当前用户量下出问题的影响。先做好三层降级保底，多模型切换是下一步。

---

### Q14: 如果并发量大了，现在这个架构有什么瓶颈？

**面试官在考**：你有没有提前想过扩展性问题，以及解决路径是什么。

> 最大瓶颈：每次请求从 MySQL 全量拉商品 → 构建 ShoeRetriever（向量化所有商品）→ 传给 Agent。当前几十个商品没问题，但上万商品后，每次请求都做一次全量向量化会变成性能瓶颈。
>
> 改进路径（按优先级排）：
> 1. 商品和向量索引在 Python 启动时加载到内存，定时（比如每分钟）从 MySQL 增量更新。请求时直接读内存，不再查库
> 2. 对话历史从内存字典迁到 Redis——现在服务重启所有对话丢失，而且不支持多实例部署
> 3. Java ↔ Python 用 Protobuf 或 MessagePack 替代 JSON——商品列表大时序列化开销显著
> 4. 热门查询结果缓存——"500 以内跑鞋"这种高频搜索，不用每次都过一遍 Agent
>
> 目前单机日活几百完全没问题，这些优化是为千级以上做准备的。

---

### Q15: Token 消耗怎么控制？你考虑过成本吗？

**面试官在考**：你对 LLM 的成本有没有概念，有没有做过优化。

> 没有做激进优化——当前用户量小，每天几十条对话，token 成本在几毛钱级别。但我清楚成本的构成和控制方向：
>
> 当前已有的控制：
> - system prompt 控制在 ~400 字，不写长篇指令
> - 商品搜索返回 top 8 给 LLM（tool 里 `items[:8]`），不是全量
> - 对话历史滑动窗口保留最近 10 轮
> - RAG 知识块返回 top 3，不塞太多
>
> 还没做但值得做的：
> - Prompt Caching：DeepSeek 如果支持的话，system prompt 和商品描述这些不变的内容可以缓存，每次只需要输入 user message，成本降 50%+
> - 用户消息预处理：用本地小模型先把"我今天心情好想买双鞋犒劳自己穿着去海南旅游"精简成"需求：旅游鞋，海南，休闲"，再发给 Agent——减少无关文本的 token
>
> 我的原则：先验证产品价值，再优化成本。如果产品没人用，token 优化得再好也没意义。

---

### Q16: 前端为什么选 Vue 3 CDN 而不是 React + 构建工具？

**面试官在考**：你的选型是理性判断，还是"只会这个"。

> 四个页面（登录、注册、首页、个人中心）+ 未来可能加一个数据看板。没有构建步骤——几个 HTML 文件引入 Vue CDN，部署就是复制到 Spring Boot 的 static 目录。
>
> 不选 React + Vite 脚手架的原因：项目体量下，脚手架带来的收益（组件复用、热更新、Tree Shaking）远小于它增加的心智负担（配置 Vite、管理 node_modules、处理 SSR/CSR、部署时还要多一个构建步骤）。
>
> Vue CDN 的响应式模板刚好够——登录表单的状态管理、首页的聊天消息列表、个人中心的 tab 切换，用 Vue 的 `v-model`/`v-for`/`v-if` 就够了。不需要 Pinia/Vuex 做全局状态管理，因为页面之间没有共享状态。
>
> 设计上：参考了轻奢电商风格——玻璃态（backdrop-filter blur）、CSS 动画轮播、数字滚动、粒子效果。全都是纯 CSS/JS 实现，零前端依赖。
>
> 但如果以后复杂度增加（多页面路由、组件复用、权限控制），我会迁移到 Vite + Vue SFC。这个决策不是"我不会 React"，而是"在这个项目规模下 Vue CDN 刚好够用"。

---

### Q17: 这个项目最大的不足是什么？

**面试官在考**：你有没有工程成熟度意识，能不能诚实面对问题。

> 最大的不足：没有量化的推荐效果评估。我现在判断"推荐好不好"全靠自己手动测试——输入几个问题、看看 AI 回什么、觉得"还行"就过了。这在大二做项目可以，但在工程上不够——我需要知道：
> - 推荐 → 收藏的转化率是多少？
> - 用户点了 👍 还是 👎？
> - AI 给的追问建议，用户真的点了吗？
> - 同一段 system prompt，改了之后效果变好还是变差？
>
> 没有这些指标，我就不知道自己的改动是"优化"还是"劣化"——改了代码觉得更好了，但用户可能觉得更差了。
>
> 下一步最想做的就是加评测体系——先用已有的 `ai_feedback` 表统计 👍/👎 比例和收藏转化率，再用 LLM-as-Judge 定期抽样给推荐打分。同时也需要可观测性——每次 Agent 调用的工具链路、延迟、token 数——出问题时能定位是哪个环节慢了。

---

### Q18: 如果重新做这个项目，你会改什么？

**面试官在考**：你有没有从过程中学到东西，能不能跳出当前代码看问题。

> 两件事：
>
> 1. 先做可观测性再写功能。现在加一个功能，我不知道它对延迟和 token 成本的影响是多少——全靠人工感觉。如果重新来，第一件事就是在 Agent 调用链上埋点：每个请求的 trace_id、工具调用耗时、首 token 延迟、总 token 数。功能可以不完美，但数据要有——有了数据才能做决策。
>
> 2. Java 和 Python 之间用更结构化的通信方式。现在 Java 手写 JSON 序列化（`productToJson` 那个方法 30 行），Python 用 Pydantic 反序列化。两边各维护一套数据结构，加一个字段要改两处。如果重新来，用 Protobuf 定义一份 `.proto` 文件，两边自动生成代码——单点维护，不会出现字段名对不上的 bug。
>
> 话说回来，当时手写 JSON 是因为项目初期结构还在变，Protobuf 的编译步骤会拖慢迭代速度。现在结构稳定了，再引入 Protobuf 是合理的。

---

## 四、答题技巧速查

### 记住三个模式

**1. "结论 → 细节 → trade-off" 三层结构**

❌ "我用了语义检索"
✅ "因为关键词匹配解决不了语义鸿沟——'膝盖不好'和'减震跑鞋'字面不同但语义相关——所以我用 SentenceTransformer 做向量化。代价是内存多占 ~400MB（加载模型），但和引入了 Elasticsearch 相比，这个代价可以接受"

**2. 主动说"替代方案"和"为什么没选"**

面试官没问你也该说：
> "我考虑过 A，但 X 不成立。也想过 B，但 Y 成本太高。最后选 C，因为它在 X 和 Y 之间做了平衡。"

这个回答模式证明你不是随手选的技术，而是做过 trade-off 分析。

**3. 诚实说出不足，但附带改进路径**

❌ "没有不足，都挺好的"（面试官内心：这人要么没思考，要么不诚实）
✅ "当前最大的不足是没有量化的评测体系。下一步计划用业务数据（收藏率、反馈率）+ LLM-as-Judge 做效果评估"（面试官内心：这人有工程成熟度意识）

### 三个绝对不要说

- "这是一个课程作业"——它会立刻把项目的档次拉低到"为了交差"
- "教程上是这么写的，我就这么做了"——证明你没有独立思考
- "我没想过这个问题"——改说"这是我下一步要做的事"

### 一个底层心法

面试官不是在找"正确的技术"，而是在找**有工程判断力的人**。你做的每个技术选择都有一个"为什么"——把这个"为什么"讲清楚，比技术本身重要十倍。

---

## 五、面试前一天快速自查

- [ ] 能不能一句话说清楚项目价值？（不是"用了什么技术"，是"帮用户解决了什么"）
- [ ] 能不能解释 Agent 和直接调 API 的本质区别？
- [ ] 能不能说出语义检索替代 LIKE/ES 的量级依据？
- [ ] 能不能画出 SSE 的完整链路并说出每个环节的坑？
- [ ] 能不能解释"收藏 > 浏览"的信号质量逻辑？
- [ ] 能不能诚实说出三个不足 + 每个的改进方向？
- [ ] 写的最有技术含量的代码是哪段？（建议讲 Java SSE 透传或 Agent 工具设计）
- [ ] 最大的坑是什么？怎么解决的？（建议讲 RestTemplate 缓冲了 SSE 流）
- [ ] 这个项目让你学到最重要的是什么？（建议讲"理解了 Agent 是把决策权从代码转移给 LLM"）
- [ ] 有没有想过"LLM 本来就有知识，为什么还要语义检索 + 知识库"？
