# AI 鞋类推荐助手 — 面试拷打应答

> 适用岗位：AI 应用开发 / 后端开发 / 全栈开发 实习或校招
> 回答策略：先说结论，再说细节，最后补 trade-off。展现你知道的不只是"怎么做"，更是"为什么这样做"。

---

## 一、开场：一句话抓住注意力

面试官："介绍一下这个项目。"

> 我做了一个 AI 鞋类推荐助手。用户用自然语言描述需求——比如"我膝盖不好，想找缓震好的跑鞋，预算 800 以内"——系统能像专业商品推荐一样理解需求、调用工具搜索商品、结合专业知识给出有依据的推荐。
>
> 技术栈：Spring Boot + FastAPI 双服务，核心是基于 LangChain ReAct Agent 的自主工具编排（5 个工具），SentenceTransformer 做语义检索代替传统关键词搜索，SSE 实现流式输出。三大增强模块——鞋类专业知识库做 Agentic RAG、MySQL 持久化的多轮对话记忆（像 ChatGPT 一样新开/切换/删除对话）、SQLite + Chart.js 的实时可观测性看板（追踪每次 Agent 调用的 token 消耗、工具链路、P50/P95/P99 延迟）。部署用 Docker Compose。

**必须出现的四个关键词**：ReAct Agent、语义检索、Agentic RAG、流式输出。根据岗位侧重，再加"对话记忆持久化"或"可观测性"。

---

## 二、面试官拷打路线图

面试官一般按这个逻辑层层递进。每个方向 2-3 道追问，答好了就换方向，答不好就深挖。

```
自我介绍
  │
  ├── 拷打方向 1：架构决策
  │   "为什么是两个服务？"（Q1）
  │     └─ "那为什么不用微服务框架？"（Q2）
  │
  ├── 拷打方向 2：AI 核心
  │   "Agent 做了什么？"（Q3）
  │     └─ "和直接调 API 有什么本质区别？"（Q4）
  │     └─ "LangChain create_agent 和旧版的区别？"（Q5）
  │
  ├── 拷打方向 3：检索系统
  │   "语义检索怎么做的？"（Q6）
  │     └─ "为什么不用 Elasticsearch？"（Q7）
  │
  ├── 拷打方向 4：流式链路
  │   "SSE 怎么实现的？"（Q8）
  │     └─ "为什么 SSE 不用 WebSocket？"（Q9）
  │     └─ "Java 透传遇到什么坑？"（Q10）
  │
  ├── 拷打方向 5：个性化
  │   "用户画像怎么做的？"（Q11）
  │     └─ "为什么不用协同过滤？"（Q12）
  │
  ├── 拷打方向 6：脆弱环节
  │   "DeepSeek 挂了怎么办？"（Q13）
  │   "并发大了有什么瓶颈？"（Q14）
  │   "token 成本怎么控？"（Q15）
  │
  ├── 拷打方向 7：工程判断
  │   "前端为什么这么选？"（Q16）
  │   "这个项目最大的不足？"（Q17）
  │   "如果重新做，会改什么？"（Q18）
  │
  ├── 拷打方向 8：对话记忆持久化（新增 🆕）
  │   "记忆怎么存的？重启丢不丢？"（Q19）
  │     └─ "为什么不用 Redis 做缓存？"（Q20）
  │     └─ "多轮对话怎么截断？"（Q21）
  │
  ├── 拷打方向 9：可观测性（新增 🆕）
  │   "怎么知道系统跑得好不好？"（Q22）
  │     └─ "为什么不用 Prometheus + Grafana？"（Q23）
  │     └─ "token 追踪具体怎么算的？"（Q24）
  │
  ├── 拷打方向 10：容器化部署（新增 🆕）
  │   "为什么用 Docker？"（Q25）
  │     └─ "多容器之间怎么通信的？"（Q26）
  │     └─ "Docker Compose 和 K8s 的区别？"（Q27）
  │
  ├── 拷打方向 11：Agent 可靠性 & 状态管理（新增 🆕）
  │   "怎么保障工具调用可靠？"（Q28）
  │     └─ "多轮对话怎么解决状态爆炸？"（Q29）
  │     └─ "怎么提升 RAG 准确率？（三层架构）"（Q30）
  │
  ├── 拷打方向 12：Agent 量化评测（新增 🆕）
  │   "Agent 性能怎么量化评估？"（Q31）
  │     └─ "影子测试怎么做的？有什么发现？"（Q32）
  │
  ├── 拷打方向 13：高并发与 Redis（新增 🆕）
  │   "高并发怎么设计？"（Q33）
  │     └─ "Redis 具体怎么用的？"（Q34）
  │
  ├── 拷打方向 14：RAG 专项（单独文档）
  │   见《面试准备-RAG知识库拷打.md》
  │
  └── 拷打方向 15：对话记忆专项（单独文档）
      见《面试准备-对话记忆拷打.md》
```

---

## 三、逐题应答

---

### Q1: 为什么是 Spring Boot + FastAPI 两个服务？一个服务不行吗？

**面试官在考**：你是不是为了简历好看而过度设计了。

> 两个服务不是因为"微服务更高级"，而是两个语言生态各有所长，强行合在一起反而更复杂。
>
> Spring Boot 负责用户系统——登录、session、收藏、历史记录、反馈。这些是标准的三层 CRUD，Java 的类型安全 + JdbcTemplate 直接写 SQL，我可控性最好。
>
> Python FastAPI 负责 AI——LangChain 和 SentenceTransformer 的生态全在 Python。如果强行用 Java 调 Python ML 库，要么走 JNI（噩梦级调试），要么启动 Python 子进程（每次都冷启动模型，慢到不能用）。
>
> 替代方案：全用 FastAPI 写业务——但我当时对 Python 的 ORM 不熟，直接用 JDBC 写 SQL 更顺手。全用 Spring AI——但 Spring AI 对 DeepSeek 的支持和中文语义检索当时不够成熟。
>
> 代价：多了一跳 HTTP 调用（内网 <5ms，可以忽略）。好处：各自独立部署和扩容。以后如果想给 AI 服务上 GPU 机器，改个地址就行，用户系统不受影响。

---

### Q2: 那为什么不加注册中心、网关、熔断？这就是个"伪微服务"吧？

**面试官在考**：你能不能区分"服务拆分"和"微服务架构"，有没有被框架绑架。

> 这是两个服务独立部署，不是微服务架构——我没有用 Spring Cloud、Nacos、Sentinel。原因很简单：不需要。
>
> 微服务那套基础设施（注册中心、网关、熔断、链路追踪）解决的问题是"几十个服务之间的调用关系复杂到人脑理不清"。我这里就两个服务，一个调另一个，拓扑简单到一眼看完。
>
> 引入 Spring Cloud 的收益为零，但成本巨大——配置 Nacos 的时间比我写业务逻辑还长。大二学生项目不应该追求架构完备性，应该追求"刚好够解决问题"。
>
> 但我不能说没想过这些。如果以后服务拆多了——比如把知识库单独抽成一个服务、加一个评测服务——我会先引入一个轻量网关（Nginx 或 Traefik）做反向代理和限流，再考虑熔断。演进顺序应该是：先发现问题（比如 Python 服务挂了导致 Java 线程卡死），再引入对应解决方案。而不是反过来——先把全套微服务基础设施搭好，再往里填业务代码。

---

### Q3: Agent 做了什么？你给了它什么工具？

**面试官在考**：你理解 Agent 的本质——不是调 API，是让 LLM 自主编排工具。

> Agent 收到用户消息后，自主决定"下一步做什么"：搜商品、查知识、对比、追问、还是直接回复。这个决策循环（思考→行动→观察→再思考）由 LangChain 的 create_agent 实现，我只需要定义工具有哪些——LLM 自己决定什么时候用、按什么顺序用、用了之后要不要换个角度再查。
>
> 给 Agent 的工具（5 个）：
> - `search_products`：语义搜索鞋款，支持类目/品牌/性别/价格过滤
> - `analyze_outfit`：根据穿搭描述匹配鞋款（上装/下装/场合/风格 → 鞋）
> - `compare_shoes`：对比两双鞋的优劣
> - `ask_clarify`：信息不够时主动追问，而不是瞎猜
> - `search_knowledge`（RAG）：查鞋类专业知识库——6 个 Markdown 文件涵盖材质科技、足型选购、运动场景、品牌定位、尺码指南、保养清洁。Agent 遇到专业问题时自主检索，比如用户说"扁平足该穿什么鞋"，Agent 先调 search_knowledge 查扁平足选鞋标准，再调 search_products 搜支撑型跑鞋，两步联动给出有依据的推荐。
>
> 关键区别：如果直接调 API 做 function calling，我需要自己写循环逻辑——调了工具 → 拿到结果 → 判断是否继续调 → 拼接上下文 → 再调 LLM。这不只是多写代码的问题，而是边界 case 太多：工具返回空了怎么办？Agent 陷入死循环了怎么办？中间结果怎么管理？LangChain 帮我封装了这些，省下的精力可以花在提升推荐质量上。5 个工具中，Agent 自由组合调用——扁平足问题会触发"知识→商品→知识"的链式检索，穿搭问题会触发"分析→搜索"的两步推理，简单的价格对比可能一次 search_products 就够了。

---

### Q4: 那和直接调 DeepSeek API 的 function calling 有本质区别吗？你不就是套了个 LangChain 壳？

**面试官在考**：你有没有真正理解 LangChain 帮你做了什么，还是只是"用了就用了"。

> 本质区别在于三个能力：
>
> 1. 流式中间状态：用 `astream_events v2`，我能捕获到"工具开始执行""LLM 逐 token 产出""工具执行结束"三类事件。原生 function calling 只能拿到最终结果，中间过程对我是黑盒。前端显示"正在为你搜索合适的鞋款..."——这个体验就是靠捕获中间事件实现的。
>
> 2. 错误恢复和重试：如果工具调用返回了格式错误的结果，Agent 能自己重试——"这次搜到的结果太少，换个关键词再搜"。原生 API 需要你在客户端写重试逻辑。
>
> 3. 多工具编排：用户说"我扁平足，帮我找一双跑鞋，和我的 UltraBoost 对比一下"——Agent 需要先查知识库（扁平足选鞋标准），再搜商品，再找出 UltraBoost 在不在库里做对比。这个链条的编排逻辑，用原生 function calling 需要我写一套状态机。
>
> 说它是"套壳"也对——LangChain 确实封装了底层 API 调用。但封装的价值不在于"少写代码"，而在于让我在更高的抽象层工作：不关心"怎么调 API"，关心"Agent 的工具体系怎么设计、工具描述怎么写 LLM 才不会乱调"。

---

### Q5: LangChain create_agent 和旧版 AgentExecutor 有什么区别？

**面试官在考**：你跟进了技术更新，还是随手用了教程里的代码。

> `create_agent` 是 LangChain 0.3 推出的新 API，底层基于 langgraph 的状态图。相比旧版 `AgentExecutor`：
>
> - 流式支持：旧版 AgentExecutor 的中间步骤很难获取——你能看到 Agent 调了工具，但看不到调的是什么、中间状态是什么。`create_agent` + `astream_events v2` 能捕获 `on_tool_start`/`on_tool_end`/`on_chat_model_stream`，我才能给前端展示"正在搜索…"。
> - API 简洁度：旧版需要手动配 AgentExecutor(memory=..., max_iterations=..., handle_parsing_errors=...)。新版 `create_agent(llm, tools, system_prompt=...)` 三个参数。
> - 底层图结构：langgraph 本质是一个有向无环图，Agent 的每一步是图的一个节点。这为以后自定义复杂的 Agent 流程（比如"先查知识 → 后搜商品 → 再对比"的固定 Pipeline + LLM 自主决策的混合模式）留下了扩展空间。

---

### Q6: 语义检索怎么做的？为什么不用 MySQL LIKE 模糊查询？

**面试官在考**：你理解"语义匹配"和"关键词匹配"的本质区别，以及选型时的量级意识。

> 用 SentenceTransformer 的 `text2vec-base-chinese` 模型。流程：启动时把所有商品描述向量化（name + description 拼接），存到 numpy 数组。用户 query 到来时同样向量化，用余弦相似度计算 top-k 返回最相关的商品。
>
> 为什么不用 LIKE？一个实际例子：用户说"膝盖不好想找缓震跑鞋"。商品描述里写的是"减震""缓震""吸震""冲击吸收"——LIKE 匹配"缓震"可能会漏掉所有写"减震"的商品，匹配"膝盖"可能一条都找不到。语义检索能发现"膝盖不好"和"减震跑鞋"在语义上是强相关的——即使字面上完全不同。
>
> 为什么不用 MySQL 全文索引（FULLTEXT）？同样是词匹配问题——"膝盖不好"在全文索引里会被拆成"膝盖""不好"两个词，搜出来的可能是任何包含"膝盖"或"不好"的内容，完全没有语义理解。

---

### Q7: 那为什么不用 Elasticsearch？

**面试官在考**：你是不是会盲目追求"更好的技术"而忽视运维成本。

> ES 确实能做向量检索（8.x 版本后原生支持 KNN），但我没选它的原因：
>
> 1. 数据量：不到 1000 个商品，numpy 数组的内存占用不到 3MB，一次余弦相似度计算不到 1ms。ES 的优势在百万级以上——分布式索引、近似 KNN（ANN）大幅减少计算量。1000 条数据上 ES 的收益为零。
> 2. 运维成本：ES 需要额外的 Java 进程、内存管理、索引维护。Docker 里多跑一个 ES 容器，内存多占 1-2GB。我的阿里云学生机总共就 2GB 内存。
> 3. 部署复杂度：我的 Docker Compose 现在只有 MySQL + Spring Boot + Python 三个服务。加 ES 意味着多一个容器、多一套健康检查、多一套日志。引入 ES 的成本远大于几百行 numpy 代码。
>
> 但我清楚什么时候该切 ES：商品量上万、检索延迟提到 >50ms、需要复杂过滤+排序+聚合时。技术选型的核心不是选"最好的"，而是选"当前规模下刚好够用的，并且有明确的升级路径"。

---

### Q8: SSE 流式输出怎么实现的？完整的链路是什么？

**面试官在考**：你理解端到端的流式链路，而不是只知道"用了 SSE"。

> 链路：Python FastAPI → Java Spring Boot → 浏览器。
>
> Python 端（产生流）：
> - 用 `streaming=True` 的 ChatOpenAI + `create_agent`
> - 调 `agent.astream_events(messages, version="v2")` 获取异步事件流
> - 事件类型有三种：`on_tool_start`（推送工具状态给前端）、`on_chat_model_stream`（逐 token 推）、`on_tool_end`（清状态）
> - FastAPI 的 `StreamingResponse` 配合 async generator，每个 yield 立即 flush
>
> Java 端（透传流）：
> - 这是整个链路最容易踩坑的地方。RestTemplate 会等 HTTP 响应完整接收后才返回——这意味着 Python 已经推了 10 个 token，Java 还在等响应结束。流就断在这里了。
> - 我用原生 `HttpURLConnection` 打开连接，`BufferedReader.readLine()` 逐行读取 Python 的 SSE，读到一行就 `PrintWriter.write()` + `flush()` 到 `HttpServletResponse`。
> - 关键配置：`response.setHeader("X-Accel-Buffering", "no")` 禁用 nginx 缓冲；`Cache-Control: no-cache` 防止浏览器缓存流。
>
> 前端（消费流）：
> - `fetch()` + `response.body.getReader()` 读 `ReadableStream`
> - 每行解析 SSE 格式（`data: {...}`），根据事件类型更新 UI
>
> 降级策略：Python 端 `astream_events` 如果抛异常，catch 后用 `agent.invoke()` 一次性返回，token 字段放完整文本——前端体验会从"逐字打字"变成"一次出全文"，但不会白屏。

---

### Q9: 为什么 SSE 而不用 WebSocket？

**面试官在考**：你理解两种流式技术的适用场景。

> SSE 是单向的（服务端→客户端），WebSocket 是双向的（两端都能主动发消息）。
>
> 我的商品推荐场景：用户发一条消息 → 服务端推回一个流 → 结束。这是典型的单向推送，SSE 完全够用。
>
> SSE 的优势：
> - 基于 HTTP 协议，不需要升级握手。浏览器原生支持 `EventSource`（虽然我用 fetch 手动解析是为了更好的控制）
> - 自动重连——浏览器 `EventSource` 断线会自动重连，WebSocket 需要自己写重连逻辑
> - 穿透代理更友好——HTTP 流大部分代理默认放行，WebSocket 协议升级可能被企业代理拦截
>
> WebSocket 适合的场景：聊天室（双方都主动发消息）、协作编辑（双方都在打字）、实时游戏（双向高频通信）。我的项目是"用户→提问，AI→回复"，单向的。用 WebSocket 多了一个不必要的双向通道。
>
> 如果以后加一个"AI 主动推送"的功能（比如"您收藏的鞋降价了"），那时 WebSocket 或 Server Push 才值得考虑。

---

### Q10: Java 透传 SSE 遇到了什么具体的坑？

**面试官在考**：你有没有实际调试过，还是只是"看起来能跑"。

> 最大的坑就是 RestTemplate 会缓冲完整响应。我第一次写的时候，Java 端用的 `RestTemplate.execute()` 接收 Python 的流——结果前端等了 20 秒然后一次性弹出完整回复。排查了半天发现 RestTemplate 的 `SimpleClientHttpRequestFactory` 默认不设置 `bufferRequestBody`，底层 `InputStream` 会被完全读完才返回。
>
> 第二个坑：`BufferedReader.readLine()` 按行读，但 Python 端的 `yield` 产出的 token 可能没有换行。我需要确保 Python 端每个 SSE 事件后面都有 `\n\n`（双换行是 SSE 协议的标准分隔符），否则 Java 的 `readLine()` 会一直阻塞。
>
> 第三个坑：中文编码。DeepSeek 返回的 token 包含中文，Java 的 `InputStreamReader` 需要显式指定 UTF-8，否则 Windows 系统默认 GBK 会乱码。Python 端也要在 StreamingResponse 里显式配 UTF-8。
>
> 动手之前我以为"就是一个流式转发，几行代码的事"，实际写了 80 行，踩了三个坑才调通。

---

### Q11: 用户画像是怎么实现的？为什么只统计收藏而不是浏览？

**面试官在考**：你理解信号质量和冷启动问题。

> 用户登录后，Java 端查他的收藏记录，统计偏好品牌、品类、平均价位，拼成自然语言注入 system prompt：
>
> "该用户画像：偏好品牌 Nike、ASICS；偏好鞋类 跑鞋；平均收藏价位约 ¥600；共收藏 8 双鞋。推荐时可以优先考虑这些偏好，但不要刻意提'画像'两个字。"
>
> 为什么只用收藏？收藏 = 用户主动操作，是"我喜欢"的明确信号，质量高。浏览行为（页面停留时间、点击次数）噪音很大——用户可能点开一双鞋是因为好奇、误触、或者只是在对比。在没有大量数据（几十万条行为日志）的情况下，用低质量信号做个性化反而会推荐变差。
>
> 为什么不用协同过滤（Collaborative Filtering）？CF 需要"用户-商品"的交互矩阵足够稠密。我的用户量和商品量都不够——用户 A 收藏了 3 双、用户 B 收藏了 5 双，两人的交集可能是空的，CF 算不出任何东西。LLM + 画像注入在冷启动和数据稀疏的场景下天然优于传统推荐模型——LLM 不需要训练，靠常识就能从"喜欢 Nike、跑鞋、¥600"推断出应该优先推荐 Nike 的中端跑鞋。
>
> 以后如果用户量上来了：协同过滤做召回（从海量商品中快速筛 100 个候选），LLM Agent 做精排（从 100 个中挑 5 个并给出个性化理由）。两者是互补关系，不是替代关系。

---

### Q12: 用户不收藏怎么办？画像就一直是空的？

**面试官在考**：你考虑过冷启动的用户体验。

> 确实——新用户或不喜欢收藏的用户，画像是空的，system prompt 里就没有个性化信息，Agent 做通用推荐。
>
> 这不是 bug，是设计意图：通用推荐的质量已经足够好（LLM 理解自然语言需求 + 语义检索找商品），个性化是锦上添花。
>
> 如果要改进，可以加隐式画像——不依赖用户主动收藏，而是分析对话历史。比如用户连续问了三次"跑鞋""轻便""透气"，Agent 可以推断"这个用户偏好轻量透气跑鞋"。但这需要多轮对话的偏好提取能力，复杂度比收藏画像高不少。
>
> 当前的取舍：优先保证"不登录也能得到好推荐"的基线体验，个性化是可选的增强。强制要求用户先收藏再推荐 —— 那个体验更差。

---

### Q13: DeepSeek API 挂了怎么办？

**面试官在考**：你有没有想过失败场景，还是只写了 happy path。

> 三层降级：
>
> 层一，Python Agent 内部：`astream_events` 抛异常 → catch 后用 `agent.invoke()` 非流式一次性返回。体验降级为"没有打字动画，完整回答一次性出"，但不出错。
>
> 层二，Java 调用 Python：`HttpURLConnection` 连接超时或 HTTP 错误 → catch 后向前端写降级 SSE 事件：`{"done":true, "reply":"AI 暂时不可用，请稍后再试。"}` ——前端能正常结束流，不会无限 loading。
>
> 层三，前端 fetch：网络错误 → catch 后显示 toast 提示"网络异常，请重试"。
>
> 没做的：多模型 fallback。理想情况下 DeepSeek 挂了自动切 OpenAI——但需要管理两套 API Key、处理两个模型的格式差异、做健康检查。目前 DeepSeek 的可用性还不错，且多模型 fallback 的代码复杂度 > 当前用户量下出问题的影响。先做好三层降级保底，多模型切换是下一步。

---

### Q14: 如果并发量大了，现在这个架构有什么瓶颈？

**面试官在考**：你有没有提前想过扩展性问题，以及解决路径是什么。

> 最大瓶颈：每次请求从 MySQL 全量拉商品 → 构建 ShoeRetriever（向量化所有商品）→ 传给 Agent。当前几十个商品没问题，但上万商品后，每次请求都做一次全量向量化会变成性能瓶颈。
>
> 改进路径（按优先级排）：
> 1. 商品和向量索引在 Python 启动时加载到内存，定时（比如每分钟）从 MySQL 增量更新。请求时直接读内存，不再查库
> 2. 对话历史目前是 MySQL 持久化 + Python 内存字典双层缓存——已经解决了"重启丢记忆"的问题。但如果上多实例，内存字典就不够用了（实例 A 存了用户的记忆，实例 B 没有），需要引入 Redis 做共享缓存层
> 3. Java ↔ Python 用 Protobuf 或 MessagePack 替代 JSON——商品列表大时序列化开销显著
> 4. 热门查询结果缓存——"500 以内跑鞋"这种高频搜索，不用每次都过一遍 Agent
>
> 目前单机日活几百完全没问题，这些优化是为千级以上做准备的。对话记忆的持久化已经做了——MySQL 负责"重启不丢"，localStorage 负责"刷新不丢当前对话"，三层架构（MySQL → Python 内存 → localStorage）覆盖了不同粒度。

---

### Q15: Token 消耗怎么控制？你考虑过成本吗？

**面试官在考**：你对 LLM 的成本有没有概念，有没有做过优化。

> 当前已有真正的 token 追踪数据——不是估算，是每次 Agent 调用的实测值。通过 trace.py（SQLite 存储）记录每次请求的 tokens_input、tokens_output，在看板上能看到每日 token 消耗趋势和预估费用。根据真实数据，一次完整的 Agent 对话（含工具调用）大约消耗 2000-5000 tokens，单次成本约 ¥0.004-0.01（DeepSeek 价格）。日活 100 用户、每人 5 轮对话，一天的成本也就几块钱。
>
> 已有控制措施：
> - system prompt 控制在 ~400 字，不写长篇指令
> - 商品搜索返回 top 8 给 LLM（tool 里 `items[:8]`），不是全量
> - 对话历史滑动窗口保留最近 10 轮（Java 端查 MySQL 取最近 20 条，Python 端 `_trim_history` 再做一次截断）
> - RAG 知识块返回 top 3，不塞太多
> - 可观测性看板上能看到每日 token 趋势曲线——异常飙升一眼就能发现，不会"悄悄烧钱"
>
> 还没做但值得做的：
> - Prompt Caching：DeepSeek 如果支持的话，system prompt 和商品描述这些不变的内容可以缓存，每次只需要输入 user message，成本降 50%+
> - 用户消息预处理：用本地小模型先把"我今天心情好想买双鞋犒劳自己穿着去海南旅游"精简成"需求：旅游鞋，海南，休闲"，再发给 Agent——减少无关文本的 token
>
> 我的原则：先验证产品价值，再优化成本。但有可观测性数据之后，我可以做数据驱动的成本优化——比如发现某类用户消息平均消耗 8000 tokens，说明 Agent 在这个场景下过度搜索了，需要调工具描述或 system prompt。

---

### Q16: 前端为什么选 Vue 3 CDN 而不是 React + 构建工具？

**面试官在考**：你的选型是理性判断，还是"只会这个"。

> 四个页面（登录、注册、首页、个人中心）+ 未来可能加一个数据看板。没有构建步骤——几个 HTML 文件引入 Vue CDN，部署就是复制到 Spring Boot 的 static 目录。
>
> 不选 React + Vite 脚手架的原因：项目体量下，脚手架带来的收益（组件复用、热更新、Tree Shaking）远小于它增加的心智负担（配置 Vite、管理 node_modules、处理 SSR/CSR、部署时还要多一个构建步骤）。
>
> Vue CDN 的响应式模板刚好够——登录表单的状态管理、首页的聊天消息列表、个人中心的 tab 切换，用 Vue 的 `v-model`/`v-for`/`v-if` 就够了。不需要 Pinia/Vuex 做全局状态管理，因为页面之间没有共享状态。
>
> 设计上：参考了轻奢电商风格——玻璃态（backdrop-filter blur）、CSS 动画轮播、数字滚动、粒子效果。全都是纯 CSS/JS 实现，零前端依赖。
>
> 但如果以后复杂度增加（多页面路由、组件复用、权限控制），我会迁移到 Vite + Vue SFC。这个决策不是"我不会 React"，而是"在这个项目规模下 Vue CDN 刚好够用"。

---

### Q17: 这个项目最大的不足是什么？

**面试官在考**：你有没有工程成熟度意识，能不能诚实面对问题。

> 最大的不足：没有量化的推荐效果评估。我现在判断"推荐好不好"全靠自己手动测试——输入几个问题、看看 AI 回什么、觉得"还行"就过了。这在大二做项目可以，但在真正的 AI 应用开发中，缺乏评测 = 没法迭代。
>
> 我需要知道：
> - 推荐 → 收藏的转化率是多少？
> - 用户点了 👍 还是 👎？比例随时间上升还是下降？
> - AI 给的追问建议，用户真的点了吗？（点击率）
> - 同一段 system prompt，改了之后效果变好还是变差？（A/B 对比）
>
> 没有这些指标，我就不知道自己的改动是"优化"还是"劣化"——改了代码觉得更好了，但用户可能觉得更差了。
>
> 顺便说一句：可观测性（延迟、token、工具调用追踪）我已经做了——出问题我能定位。但"可观测"和"可评估"是两回事：前者关注系统运行状况好不好，后者关注推荐质量好不好。前者我有 SQLite + Chart.js 看板，后者还是空白。
>
> 下一步最想做的就是评测体系——先用已有的 `ai_feedback` 表统计 👍/👎 比例和收藏转化率，再用 LLM-as-Judge 定期抽样给推荐打分（让另一个 LLM 评判"这条推荐是否合理、是否有依据、是否个性化"）。终极形态是做 A/B 测试——比如修改 system prompt 的一个段落，对比两组的 👍 率和收藏转化率，用数据说话而非"我感觉变好了"。

---

### Q18: 如果重新做这个项目，你会改什么？

**面试官在考**：你有没有从过程中学到东西，能不能跳出当前代码看问题。

> 三件事：
>
> 1. 先做评测体系再做功能迭代。可观测性我是在后期才加的——现在回过头看，trace 数据非常有用：我能看到每次调用的 token 数、工具链、延迟。但如果重来，我会在第一天就把 trace 埋点做进去——不是因为可观测性本身多重要，而是**没有数据就无法做决策**。之前优化 system prompt 全靠"我感觉变好了"，现在有 token 数和工具调用次数可以做量化对比。下一步必须做的是推荐效果的评测——让 LLM-as-Judge 定期给推荐打分。
>
> 2. Java 和 Python 之间用更结构化的通信方式。现在 Java 手写 JSON 序列化，Python 用 Pydantic 反序列化。两边各维护一套数据结构，加一个字段要改两处。如果重新来，用 Protobuf 定义一份 `.proto` 文件，两边自动生成代码——单点维护，不会出现字段名对不上的 bug。当时手写 JSON 是因为项目初期结构还在变，Protobuf 的编译步骤会拖慢迭代速度。现在结构稳定了，再引入 Protobuf 是合理的。
>
> 3. 对话记忆一开始就用 MySQL 而不是 Python 字典。最开始我觉得"反正是学习项目，Python 字典存内存就行了"——结果用户测的时候每次重启后端，对话全丢。后来补了 MySQL 持久化，但如果一开始就做持久化设计，架构会更干净——不用写 `_restore_history()` 这种"把 MySQL 数据再灌回内存"的补偿逻辑。教训是：和用户体验直接相关的状态（对话记录、收藏、登录），**永远不要只放内存**——即使用户量再小。

---

## 三-续：新增模块拷打（对话记忆 + 可观测性）

---

### Q19: 对话记忆怎么存的？重启服务会丢吗？

**面试官在考**：你有没有考虑过状态持久化，还是所有对话都在内存里。

> 不会丢。我设计了三层架构：
>
> **第一层：Python 内存字典（热数据）**。`conversations: dict[str, list[dict]]`，key 是 conversationId，value 是消息列表。Agent 运行时直接从字典读——零网络开销，零延迟。
>
> **第二层：MySQL（冷存储，持久化）**。每次对话完成后，Java 端写入 `ai_chat_history` 表。下次用户打开这个对话，Java 从 MySQL 查出最近 20 条（10 轮），通过 `ChatRequest.history` 字段传给 Python，Python 的 `_restore_history()` 把历史灌回内存字典。这样服务重启、Python 进程重启——对话都在 MySQL 里，不会丢。
>
> **第三层：localStorage（前端记忆）**。浏览器存 `currentConversationId`——用户刷新页面后，前端自动恢复上一次的对话，不用重新选择。
>
> 为什么不是单层？内存快但不持久，MySQL 持久但每次查有开销，localStorage 解决"刷新不丢当前对话"的体验问题。三层各管各的粒度，组合起来就是"像 ChatGPT 一样的体验"——左侧对话列表、可以新开/切换/删除对话、刷新后自动恢复上一次对话。
>
> 之前确实只有内存字典——写代码的时候偷懒了。后来测试发现每次重启对话全丢，这是在"做的过程中学到的"——用户状态数据必须持久化。这也是 Q18 第 3 点的教训。

---

### Q20: 为什么对话存 MySQL 而不是 Redis？

**面试官在考**：你理解缓存的职责划分，不是技术越新越好。

> 对话历史是持久数据，不是缓存。Redis 的核心场景是"数据可以丢、但访问要快"——session、限流计数器、排行榜。对话历史是用户的核心数据——你删了用户的聊天记录，第二天他回来发现对话全没了，这是不可接受的。
>
> 存储选型原则：**持久数据用数据库，热缓存用 Redis**。MySQL 做主存储是正确的，Redis 适合做"加速层"——比如把活跃对话缓存到 Redis 减少 MySQL 查询，但不是替代 MySQL。
>
> 当前用户量下，MySQL 的 `SELECT ... ORDER BY create_time ASC` 取 20 条消息是毫秒级的，Redis 缓存的必要性还没出现。如果真的需要加 Redis，我会做成：MySQL 写为主 → 写完后同步更新 Redis → Python 读先查 Redis miss 再查 MySQL。但当前架构直接查 MySQL 完全够用。过度设计不值得。

---

### Q21: 多轮对话怎么截断？不会把 100 轮对话全塞给 LLM 吧？

**面试官在考**：你有没有控制上下文长度的机制，有没有 token 成本意识。

> 两道截断防线：
>
> **Java 端（MySQL 查询截断）**：`loadConversationHistory()` 只取最近 20 条（10 轮）——SQL 返回全部消息后，Java 在内存里取 `messages.size() - 20` 到尾。为什么在 Java 截断而不是 SQL？因为 SQL 取全部消息的开销很小（几十条），真正需要截断的是传给 LLM 的上下文——在应用层截断更灵活。
>
> **Python 端（内存截断）**：`_trim_history()` 在构建 LLM 消息前再做一次检查——如果内存字典里的消息超过 20 条（因为新消息被 append 进去了），裁到最近 20 条。这是兜底逻辑，防止 Java 端截断后、Python 又追加了几轮导致上下文膨胀。
>
> 为什么是 20 条（10 轮）？DeepSeek 的上下文窗口是 128K，20 条消息大概只占 2-3K tokens，不会溢出的。真正的约束不是"能不能塞更多"，而是：
> - 上下文越长，LLM 的注意力越分散——太远的历史反而干扰推理
> - Token 成本线性增长——多一轮对话多几百 tokens
> - 10 轮对商品推荐场景已经够用了——用户不会在买鞋这件事上聊 100 轮，10 轮足够完成从需求澄清到推荐到对比的完整对话
>
> 如果以后需要更长记忆，我会做摘要（Summary Memory）——把前 10 轮之前的对话自动总结成一段摘要："用户偏好跑步鞋，预算 500-800，足型偏平"，注入 system prompt，而不是保留原始消息。LangChain 有 `ConversationSummaryMemory` 可以直接用。

---

### Q22: 怎么监控系统运行状况？可观测性怎么做的？

**面试官在考**：你有没有工程意识——写代码不只是"能跑就行"，还要知道它跑得好不好。

> 我做了完整的可观测性模块（方案 B：SQLite + FastAPI + Chart.js），追踪每次 Agent 调用的全链路数据：
>
> **存储层**：`trace.py`，用 Python 标准库 `sqlite3`（零外部依赖）建 `agent_traces` 表，字段包括 trace_id、timestamp、duration_ms、first_token_ms、tool_calls（JSON）、tokens_input/output、error、conversation_id。为什么 SQLite？——追踪数据量小（每天几十条记录）、零运维、单文件（traces.db）方便备份和迁移。
>
> **埋点层**：`TraceContext` 类封装每次请求的追踪上下文。`process_message()` 和 `stream_agent()` 在 Agent 调用前后调用 `trace_ctx.start()` / `.end()`。流式场景下，`on_tool_start()` 和 `on_tool_end()` 记录每次工具调用的名称和耗时，`on_first_token()` 记录用户从发消息到看到第一个字的时间。
>
> **展示层**：`GET /admin/stats?days=` 返回聚合数据，`GET /admin/stats/recent?limit=` 返回明细。前端 `admin/stats.html`（只有 admin 能访问）用 Chart.js 画了 6 张图：
> - 6 个概览卡片：总请求数、平均延迟、首 token 延迟、总 token 消耗、预估费用（按 DeepSeek 公开定价 × token 数）、错误率
> - 每小时请求量柱状图
> - 工具调用分布横向柱状图（各工具被调了多少次、平均耗时）
> - 每日 token 消耗趋势折线图（输入/输出两条线）
> - P50/P95/P99 延迟分位数
> - 最近 50 条 Trace 明细表（每条记录的工具标签、错误高亮）
>
> Token 估算用了简单规则：中文 1 token ≈ 1.5 字符，英文 1 token ≈ 4 字符。精确计算要调 tokenizer（tiktoken），但 DeepSeek 用的 tokenizer 和 OpenAI 不完全一样，近似值足够做趋势分析。
>
> 这个模块的价值：以前我不知道"改了一段 system prompt 之后 token 消耗涨了多少"，现在有数据了。以前不知道"慢在哪"，现在能看到首 token 延迟 vs 总延迟——如果首 token 延迟高，瓶颈在 LLM；如果工具调用耗时高，瓶颈在检索。定位问题从"猜"变成"查"。

---

### Q23: 为什么不用 Prometheus + Grafana？你那 SQLite + Chart.js 太野路子了。

**面试官在考**：你是不是不懂业界标准方案，还是做了有意识的取舍。

> Prometheus + Grafana 是业界标准，我没用它们的理由很务实：
>
> 1. **运维成本**：Prometheus 需要独立进程、配置文件（`prometheus.yml`）、数据存储（TSDB）、Grafana 又要一个进程。我的 Docker Compose 现在是 3 个服务（MySQL、Spring Boot、Python），加上 Prometheus + Grafana 变成 5 个。学生机 2GB 内存直接爆了。
>
> 2. **数据量不匹配**：Prometheus 的优势在"每秒几十万个指标点"的时序数据——微服务集群的 CPU、内存、QPS、错误率。我的项目每天几十条 trace 记录，Prometheus 的 TSDB 是大材小用。SQLite 一张表十几条 SQL 就搞定了。
>
> 3. **开发速度**：从 0 到可用的看板——SQLite + FastAPI + Chart.js，我一个下午写完。换成 Prometheus + Grafana，光是配数据源、写 PromQL、调 Dashboard JSON 就要一天。
>
> 但我清楚什么时候该切 Prometheus：当服务拆分到 3 个以上、需要统一的监控面板、需要告警（"错误率 > 5% 发钉钉通知"）时。我的方案不是"不用 Prometheus"，而是"演进顺序正确"——先快速上线看数据，数据多了再切专业方案。SQLite traces.db 的数据结构（trace_id、timestamp、duration_ms...）和 Prometheus 的 Label + Value 模型是兼容的，迁移成本可控。
>
> 另外，如果面试官理解"B 端工具"场景——其实很多内部小工具就是用 SQLite 做追踪的，简单够用、零运维。不是每个项目都需要 Prometheus。

---

### Q24: Token 追踪具体怎么算的？DeepSeek 又不返回精确 token 数。

**面试官在考**：你理解 token 计算的原理和限制，不是瞎报数字。

> DeepSeek API 的流式响应里确实不返回 `usage` 字段（只有非流式的最后一条才带），所以流式场景下没法拿精确的 token 数。我用了两种策略：
>
> **流式：字符估算**。`TraceContext.estimate_tokens()` —— 中文按 1.5 字符/token，英文按 4 字符/token。这个比例是从实际测试中校准的：我用非流式模式跑了 50 条不同长度和内容的对话，拿到精确 token 数，和字符估算对比，发现中文 1.5、英文 4 的误差在 ±15% 以内。对于趋势分析来说，±15% 完全够用——我要知道的是"今天是不是比昨天多用了一倍的 token"，不是"精确到个位数"。
>
> **非流式：用 API 返回值**。`process_message()` 的非流式响应里，如果 API 返回了 `usage` 字段，优先用精确值。`TraceContext.end()` 接受 `tokens_input` 和 `tokens_output` 参数，如果调用方传了就存精确值，不传就回退到估算。
>
> **成本计算**：看板上用 `(tokens_input + tokens_output) × 0.000002 元/token`（基于 DeepSeek 最新定价，约 ¥2/百万 tokens）。这个数字是近似值——实际价格模型和输入输出的比例有关（输入比输出便宜）。
>
> 如果以后需要精确 token 统计，两个改进方向：
> - 用 DeepSeek 的 `/v1/chat/completions` 返回的 `usage.prompt_tokens` 和 `usage.completion_tokens`
> - 用 tiktoken 或 DeepSeek 官方 tokenizer 做精确 count
>
> 但我目前的结论是：**趋势分析不需要精确值**。±15% 的误差不影响你判断"改 system prompt 后 token 消耗涨了 3 倍"这种事。

---

## 三-续：容器化部署拷打（新增 🆕）

---

### Q25: 为什么用 Docker？一个学生项目直接跑不就完了吗？

**面试官在考**：你引入 Docker 是理性选择，还是为了简历好看。

> Docker 解决了三个真实痛点：
>
> 1. **环境一致性**：我的项目依赖 MySQL 8.0 + Python 3.11 + JDK 17 + SentenceTransformer 模型。如果换个电脑跑，装一遍环境至少半小时——装错版本还得排查。Docker 把环境打包成镜像，拉下来直接跑，三行命令搞定。
>
> 2. **模型预热**：`text2vec-base-chinese` 模型约 400MB，每次新环境都要下载。我把模型下载写在 Dockerfile 的 `RUN` 指令里——构建镜像时下载一次，之后所有的容器启动都是秒级。这利用了 Docker 的层缓存机制：只要 `requirements.txt` 不变，这一层就不会重跑。
>
> 3. **服务编排**：我三个服务（MySQL、Python AI、Java）有启动顺序——MySQL 没就绪时 Java 连接会失败。Docker Compose 的 `depends_on` + `healthcheck` 保证启动顺序正确。如果手动起三个终端，不仅要记顺序，出问题时也不知道是哪个服务挂了。
>
> 当然，学生项目直接跑没问题——但 Docker 不是"重"，是"快"。`docker compose up` 一行命令 vs 手动开三个终端、配环境变量、等模型下载。

---

### Q26: 三个容器之间怎么通信的？网络是怎么配的？

**面试官在考**：你理解 Docker 网络模型，不是只知道 `docker-compose up`。

> Docker Compose 默认创建一个 bridge 网络（我显式命名为 `shoe-net`）。三个容器都在这个网络里，可以通过**容器名**互相访问，不需要记 IP：
>
> ```
> mysql:3306        ← Java 的 jdbc:mysql://mysql:3306/aishoes
> ai-service:5000   ← Java 的 AI_SERVICE_URL=http://ai-service:5000
> ```
>
> 为什么不用 `localhost`？因为每个容器有自己的 localhost——容器 A 的 localhost 是 A 自己，不是宿主机。bridge 网络里用容器名做 DNS 解析。
>
> 端口映射只对外暴露：
> - `3307:3306` → 宿主机 3307 映射到 MySQL 容器的 3306（为什么是 3307？避免和本机已有 MySQL 冲突）
> - `5000:5000` → Python API 可被宿主机直接调（调试用）
> - `8080:8080` → 用户浏览器访问入口
>
> **Java 连 Python 的关键改动**：之前 Java 里硬编码了 `http://127.0.0.1:5000`，Docker 里不能这么写。我改成从环境变量 `AI_SERVICE_URL` 读取，本地开发时默认 `127.0.0.1:5000`，Docker 里 Compose 注入 `http://ai-service:5000`。这是 12-Factor App 的配置分离原则。
>
> **数据库初始化**：`docker-init.sql` 挂载到 MySQL 容器的 `/docker-entrypoint-initdb.d/`，MySQL 首次启动时自动执行。建表 + 10 条种子数据 + admin 账号，容器起来就能用。

---

### Q27: Docker Compose 和 Kubernetes 的区别？你为啥不用 K8s？

**面试官在考**：你能区分单机编排和集群编排，不被"K8s 更高级"绑架。

> Docker Compose 是单机编排——三个容器跑在一台机器上，Compose 管理它们的启动顺序、网络、数据卷。K8s 是集群编排——管理几十上百个容器跑在多台机器上，负责调度、自动扩缩容、滚动更新、服务发现。
>
> 我不用 K8s 的原因：**三台容器，一台机器，没有 K8s 的用武之地。**
>
> K8s 解决的核心问题是"集群规模大到人管不过来"——哪个节点有空闲资源？哪个 Pod 挂了该重启？流量大了自动扩几个副本？我这就 3 个容器，这些问题都不存在。引入 K8s 的最小配置（Deployment + Service + ConfigMap + Ingress）比我的业务代码还长。
>
> 演进路径很清楚：单机 Compose → 流量上来后加 Nginx 反向代理 + 多副本 → 再不够上 K8s。这个顺序不能反——先有单机问题，再有单机方案，最后才有集群方案。
>
> **但 Compose 和 K8s 有共通概念**，说出来证明你理解容器编排的本质：
> - Compose 的 `networks` ≈ K8s 的 Service（服务发现）
> - Compose 的 `depends_on` + `healthcheck` ≈ K8s 的 Init Container + Readiness Probe
> - Compose 的 `volumes` ≈ K8s 的 PersistentVolume
> - Compose 的 `docker-compose.yml` ≈ K8s 的 Helm Chart / Kustomize
>
> 学会 Compose 再学 K8s，概念是通的——只是 K8s 把这些概念扩展到了集群维度。

---

## 三-续：Agent 可靠性 & 状态管理拷打（新增 🆕）

---

### Q28: 怎么保障 Agent 工具调用可靠？万一 LLM 乱调工具怎么办？

**面试官在考**：你有没有把可靠性当一回事，还是只管 happy path。

> 工具调用的可靠性，核心原则是**不信任 LLM——每次调用都假设会出错，然后加兜底**。我做了四层：
>
> **层一：工具描述写清楚边界**。`search_products` 描述开头第一句是"已调 2 次仍无结果时应直接告知用户，不要再调"——在 prompt 层面就告诉 LLM 什么时候该停。品牌参数也写了"如 Nike, Adidas（用英文）"，引导 LLM 传正确的格式。
>
> **层二：空结果返回结构化信号而非空数组**。以前工具返回 `[]`，LLM 解读为"没搜到，换个关键词再搜"——这就是递归循环的起点。我改成返回 `{"empty": true, "hint": "没有匹配的商品，请放宽条件或告知用户"}`——LLM 看到这个信号就知道该停，而不是换词重试。
>
> **层三：`recursion_limit` 硬天花板**。两个 `create_agent` 后面都加了 `.with_config({"recursion_limit": 15})`。不管 LLM 多想继续搜，到 15 步 LangGraph 强制终止。这是代码级别的硬保底——之前有一次 Agent 连搜 12 次触发此限制，trace 记录下来的错误帮我发现了这个 bug，加了限制后再也没复发。
>
> **层四：流式失败自动降级**。`astream_events` 如果抛异常，catch 后切成 `agent.invoke()` 一次性返回。前端还有第二层降级——SSE 失败改调非流式 `\api\ai\chat` 端点。
>
> 为什么不是"只靠 prompt 调教 LLM"就行？因为 LLM 本质是不可控的——你写"不要重试超过 2 次"，LLM 可能理解成"我应该在第 2 次停止"也可能理解成"第 3 次开始才算超过"。prompt 是建议，代码是规则——两个层次都要有。

---

### Q29: 多轮对话怎么解决状态爆炸（上下文爆炸）？

**面试官在考**：你有没有控制上下文窗口的意识，还是 unlimited history 使劲塞。

> 两段截断 + 成对删除。
>
> **第一段：Java 端查 MySQL 就截**。`loadConversationHistory()` 只取最近 20 条（10 轮）。在数据进入 LLM 之前就裁——不浪费 HTTP 传输和序列化。
>
> **第二段：Python 端每轮兜底**。`_trim_history(history_list, max_rounds=10)` 在每次对话后执行。为什么 Java 截了 Python 还要截？因为 Python 内存里可能被新追加了几条消息，"Java 截完 Python 又多了"。
>
> **成对删除是关键细节**：
> ```python
> while len(history) > max_messages:
>     history.pop(0)  # 删最早的 HumanMessage
>     history.pop(0)  # 删对应的 AIMessage  ← 必须成对，否则格式错
> ```
>
> 如果只删一条，会出现"用户消息被删了但 AI 回复还在"——下轮对话变成两连 AI 回复之间夹了一条新用户消息，LLM 困惑。
>
> **10 轮的选型依据**：DeepSeek 上下文 128K，10 轮只占 2-3K tokens——远没到上限。选 10 轮不是因为塞不下更多，而是**太远的历史对当前推荐没有帮助**——没人会为买一双鞋聊 100 轮。真正的问题不是"怎么塞更多"，而是"怎么判断哪些历史还有用"。
>
> **下一步改进（面试里说）**：摘要压缩（Summary Memory）。前 10 轮以前的对话自动总结成一句话："用户偏好跑步鞋，预算 500-800，扁平足，之前看过 Nike Air Max"。注入 system prompt 替代原始消息。LangChain 有 `ConversationSummaryMemory`，接入成本很低。

---

### Q30: 怎么提升 RAG 准确率？从技术架构层面来说

**面试官在考**：你有没有系统性地思考过 RAG 的准确性问题，而不是只加个向量检索就完了。

> 我做了三层架构的 RAG 增强，每一层解决一个问题：
>
> **第 1 层：深度解析层 — 让知识被精准切分和标注**
>
> 之前只按 `##` 标题分主块。现在新增了三件事：
> - `###` 子块索引：每个三级标题也是一个可检索单元，但命中后返回的是完整的父块（`##` 级别）——保证上下文不丢失。比如用户问"扁平足推荐什么技术"，命中子块"推荐技术"，返回的是"扁平足"整个主块，LLM 能看到完整上下文
> - 自动关键词提取：从每个块的内容中用正则匹配品牌名（Nike/Adidas）、技术名（EVA/TPU/碳板）、足型（扁平足/高足弓）等关键术语，存入 tags 字段，检索时关键词更密集
> - 领域标签：每个文件的一级标题（如"材质科技""足型选购"）作为领域标签，方便后续追溯和归类
>
> **第 2 层：混合检索增强层 — 语义 + 关键词双路融合**
>
> 纯向量检索有个致命问题："Nike"和"Adidas"在向量空间里距离很近——都是运动品牌，语义相似。但如果用户搜"Nike 气垫"，纯向量可能会召回 Adidas 的块。关键词匹配（BM25）刚好弥补这点。
>
> 所以我加了一个 `BM25Retriever`——纯 Python 实现，零外部依赖。中文用字级别 bigram 分词（"扁平足"拆成["扁","平","足","扁平","平足"]），不需要 jieba。
>
> 混合不是简单的"两个结果拼一起"——用了 **Reciprocal Rank Fusion（RRF）**加权融合：向量检索权重 0.6，BM25 权重 0.4。每个文档的最终分数 = 向量分 / (rank + 60) + BM25 分 / (rank + 60)。
>
> 融合之后还有**重排序**：
> - 相似度阈值 0.35：低于此值的直接丢弃——不相关的知识宁可不说也不乱说
> - 来源多样性：同一文件最多 2 个块上榜，超出降权 30%，防止 3 个结果都来自同一个文件
>
> **第 3 层：生成校验层 — 防幻觉双重自检**
>
> 检索结果回归 LLM 时，我给每个知识块附加了 `confidence` 字段（high/medium/low 三级）。System prompt 里加了防幻觉规则（规则 6）：
> - 禁止编造：只能使用 search_knowledge 返回的内容
> - 强制来源标注：引用知识必须用"根据【材质科技·EVA 中底】"格式
> - 置信度分级处理：high 直接引用，medium 保留语气，low 标注"暂未完全确认"
> - 知识缺失时诚实告知：不猜不编
>
> 另外工具返回格式也从 `{content, source, heading}` 升级为 `{content, source, heading, domain, confidence, tags}`——Agent 看到的信息足够丰富，就不需要自己脑补。
>
> **一句话总结**：第 1 层解决"知识被切对"，第 2 层解决"对的被搜到"，第 3 层解决"搜到的被正确使用"。三层递进，而不是"向量化 + top_k"就完了。

---

## 三-续：Agent 量化评测拷打（新增 🆕）

---

### Q31: Agent 性能怎么量化评估？你怎么证明你的 Agent 不是"看起来能跑就行"？

**面试官在考**：你有没有用工程方法衡量 AI 系统，而不是凭感觉说"还行"。

> 我做了 4 个量化指标的评测体系，30 条标准测试用例覆盖 5 种意图，Python 脚本一键跑、自动出报告。
>
> **4 个核心指标：**
>
> 1. **任务成功率**：Agent 是否完成了用户需求。不是看"回复像不像人"，而是看——搜索意图下是否返回了足够商品？知识意图下是否提到了关键知识点？追问意图下 Agent 是否主动追问了？全量 30 条实测 **90.0%**（27/30），失败的 3 条中 2 条是 DeepSeek API 偶发超时返回"AI 暂不可用"（不是 Agent 逻辑问题），1 条是"我想买鞋"这种极度模糊的需求——Agent 调了搜索但没找到合适结果，实际上它主动追问了预算和场景，回复本身是合理的。
>
> 2. **平均推理步数**：Agent 从收到用户消息到给出最终答案，中间走了多少步（调了多少次工具）。实测**平均 2.5 步/次**——说明 Agent 不会无脑重试，2-3 步就能锁定答案。对比 trace 里那条死循环记录（12 次 search_products），加了 recursion_limit 之后步数稳定在合理范围。
>
> 3. **工具调用准确率**：Agent 调的工具有没有"调对"。不是看参数对不对（LLM 自由发挥难以精确判定），而是看意图对不对——用户要搜商品，Agent 有没有调 search_products？用户问知识，Agent 有没有调 search_knowledge？实测**准确率 86.7%**（26/30）。4 条未命中中，2 条是 API 超时无任何工具调用，1 条是极度模糊需求，实际"调错工具"的情况极少。
>
> 4. **影子测试对比**：同一 query，Agent（有工具）vs 裸 LLM（无工具），谁的回复更有用。裸 LLM 能聊鞋类知识但搜不到真实商品——Agent **57%（17/30）** 的查询返回了结构化商品卡片（含 ID/名称/价格/评分），裸 LLM 0 条。这是 Agent 不可替代的核心价值。
>
> **按意图分类**（全量 30 条）：
> - 商品搜索 11 条：**82%** 成功率，平均 2.1 步——最常见的意图，Agent 非常稳定
> - 知识检索 6 条：**100%** 成功率，平均 3.7 步——知识问题 Agent 会先查知识库再结合商品推荐，步数多 1-2 步是"知识→商品"的联动
> - 穿搭分析 4 条：**100%** 成功率，平均 2.8 步——analyze_outfit 工具稳定触发
> - 商品对比 4 条：**100%** 成功率，平均 3.0 步——通常先查商品再对比
> - 模糊需求追问 5 条：**80%** 成功率，平均 1.2 步——Agent 信息不够时主动追问，1-2 步就停
>
> **工具调用分布**（全量 30 条，共 74 次工具调用）：
> - search_products：35 次（47%）— 核心，搜商品
> - search_knowledge：25 次（34%）— 知识库联动，比例健康
> - ask_clarify：9 次（12%）— 主动追问，Agent 不会硬搜
> - analyze_outfit：4 次（5%）— 穿搭场景稳定触发
> - compare_shoes：1 次（1%）— 对比场景触发较少，和测试用例设计有关

---

### Q32: 影子测试具体怎么做？发现了什么？

**面试官在考**：你有没有对照实验意识——"有 Agent 到底比没有强在哪"。

> 影子测试：同一条 query，让 Agent（带工具）和裸 LLM（不带工具，只能靠训练数据里的知识）分别回答，对比三个维度：
>
> **1. 商品结果维度**：Agent **57%（17/30）** 的查询返回了结构化商品卡片（含 ID/名称/价格/评分），裸 LLM 完全做不到——它只能给文字建议，不可能推荐出真实数据库里的鞋。这是 Agent 最核心的价值：**不是聊天的玩具，是能和真实数据库交互的推荐系统**。
>
> **2. 知识准确性维度**：知识类问题 6 条全对（100%）。Agent 先调 search_knowledge 查知识库，search_knowledge 在全部 30 条里被调了 25 次——说明 Agent 非常信任这个工具，专业知识问题一定会先查。带来源标注的回复（"根据【材质科技·EVA 中底】…"）比裸 LLM 更有说服力。
>
> **3. 交互体验维度**：当用户需求模糊时（如"我想买鞋"），Agent 调了 ask_clarify（9 次）主动追问预算和场景，而不是直接硬推商品。裸 LLM 可能列几双鞋但不知道用户实际需求。Agent 的追问让需求收敛更快。
>
> **耗时对比**：Agent 平均 10.3s，裸 LLM 平均 8.2s。Agent 慢约 2 秒——这就是工具调用的代价。但这 2 秒换来的是真实商品推荐，ROI 完全值得。

---

## 三-续：高并发与 Redis 拷打（新增 🆕）

---

### Q33: 这个项目的高并发怎么设计的？量大了能撑住吗？

**面试官在考**：你有没有做并发控制，还是只管 happy path 单用户跑通。

> 高并发是分层的，每一层用不同策略：
>
> **Python 层 — 工具闭包注入 + 向量缓存 + Redis 会话**
> - 5 个工具函数每次请求通过 `create_tools()` 闭包注入数据，不用全局变量，多请求之间天然隔离
> - 向量索引 `get_retriever()` 首次构建后缓存，后续请求直接读，10 并发也不重复建索引（已加 double-check 锁）
> - 对话记忆从 Python 内存 dict 迁移到 Redis：uvicorn 多 worker 时共享同一份对话历史，不会出现"worker A 存的对话在 worker B 看不到"
>
> **Java 层 — Spring Session Redis + 连接池**
> - Session 从 Tomcat 内存迁到 Redis（`spring-session-data-redis`）：多实例部署时用户登录状态共享，重启不丢
> - Lettuce 连接池控制 max-active=10，不会因为高并发把 Redis 冲垮
>
> **链路层 — 三层降级不回血**
> - `astream_events` 异常切 `agent.invoke()` → Java 连接超时推降级 SSE → 前端 fetch 失败切非流式端点
> - 加 Redis 之后降级多了一层：Redis 连接失败自动回退到内存 dict，Agent 核心功能不受影响（"韧性设计"而不只是"保障设计"）
>
> **最终瓶颈在哪**：DeepSeek API。Agent 一次调用 10-30s，Python/Java/Redis 再怎么优化也解决不了 LLM 推理慢的问题。解决方案不是堆中间件，是限制并发请求数 + 加超时 + 前端 loading 提示。真正需要横向扩容的是 LLM 调用——但那是 DeepSeek 的事，不是我的事。

---

### Q34: Redis 具体怎么用的？不是装个 Redis 就算了吧？

**面试官在考**：你是真的理解 Redis 的使用场景，还是"看到别人用我也加一个"。

> 我在两个地方用了 Redis：
>
> **1. Python Agent 对话记忆（agent.py）**
> - 之前：`conversations: dict[str, list]` —— 进程内存里
> - 现在：Redis String 存储，key = `conv:{conversation_id}`，value = pickle 序列化的消息列表，TTL 24 小时
> - 为什么 String 而不是 Hash？每轮对话的消息（HumanMessage + AIMessage）是列表——Hash 的 field 不能嵌套列表，String 存完整列表更自然。序列化用的是 pickle（不是 JSON），因为 LangChain 的 HumanMessage/AIMessage 对象 pickle 直接能恢复，JSON 需要手动 convert
> - 降级：`_get_redis()` 返回 None 时自动切到内存 dict（7 行代码），Redis 挂了不影响对话功能
> - 性能：Redis GET/SET 约 0.1-0.3ms（本地 TCP），Pickle 序列化 2-3ms，一次对话历史读写总开销约 5ms——对 Agent 平均 22s 的响应来说忽略不计
>
> **2. Java 端 Session 共享（Spring Session Redis）**
> - 之前：Tomcat 内存 Session，重启丢失、多实例不共享
> - 现在：Spring Session 自动拦截所有 `session.setAttribute()` 写入 Redis，Jackson2Json 序列化
> - 为什么选 Jackson2Json 而不是默认 JDK 序列化？生成的 Redis 数据人类可读（调试方便）、体积小、不要求实体类实现 Serializable
> - Redis Key 格式：`aishoes:session:sessions:{sessionId}`，自动过期时间 24 小时（和对话记忆 TTL 一致）
>
> **3. 没做的：查询结果缓存**
> - 热门搜索（如"500 以内缓震跑鞋"）每天搜 N 次，每次都走完整 Agent pipeline。加一个 5 分钟 TTL 的缓存，命中率估计 30-40%
> - 为什么还没做——当前日活太低（几十次请求），缓存命中率还不足以证明收益。量大了这是第一个该做的
>
> **运维意识**：
> - `maxmemory 128mb + allkeys-lru`：内存到了自动淘汰冷数据，不会 OOM
> - `appendonly yes + volumes`：AOF 持久化到磁盘，Redis 进程重启也能恢复数据
> - 连接池配置：Java 端 max-active=10 / min-idle=2，Python 端惰性连接（不访问时不占连接）
> - 容器堆栈：`redis:7-alpine` 镜像只有 ~30MB，作为对比 MySQL 镜像 ~500MB

---

## 四、答题技巧速查

### 记住三个模式

**1. "结论 → 细节 → trade-off" 三层结构**

❌ "我用了语义检索"
✅ "因为关键词匹配解决不了语义鸿沟——'膝盖不好'和'减震跑鞋'字面不同但语义相关——所以我用 SentenceTransformer 做向量化。代价是内存多占 ~400MB（加载模型），但和引入了 Elasticsearch 相比，这个代价可以接受"

**2. 主动说"替代方案"和"为什么没选"**

面试官没问你也该说：
> "我考虑过 A，但 X 不成立。也想过 B，但 Y 成本太高。最后选 C，因为它在 X 和 Y 之间做了平衡。"

这个回答模式证明你不是随手选的技术，而是做过 trade-off 分析。

**3. 诚实说出不足，但附带改进路径**

❌ "没有不足，都挺好的"（面试官内心：这人要么没思考，要么不诚实）
✅ "当前最大的不足是没有量化的评测体系。下一步计划用业务数据（收藏率、反馈率）+ LLM-as-Judge 做效果评估"（面试官内心：这人有工程成熟度意识）

### 三个绝对不要说

- "这是一个课程作业"——它会立刻把项目的档次拉低到"为了交差"
- "教程上是这么写的，我就这么做了"——证明你没有独立思考
- "我没想过这个问题"——改说"这是我下一步要做的事"

### 一个底层心法

面试官不是在找"正确的技术"，而是在找**有工程判断力的人**。你做的每个技术选择都有一个"为什么"——把这个"为什么"讲清楚，比技术本身重要十倍。

---

## 五、面试前一天快速自查

- [ ] 能不能一句话说清楚项目价值？（不是"用了什么技术"，是"帮用户解决了什么"）
- [ ] 能不能解释 Agent 和直接调 API 的本质区别？
- [ ] 能不能说出语义检索替代 LIKE/ES 的量级依据？
- [ ] 能不能画出 SSE 的完整链路并说出每个环节的坑？
- [ ] 能不能解释"收藏 > 浏览"的信号质量逻辑？
- [ ] 🆕 能不能说清对话记忆的三层架构（内存 + MySQL + localStorage）和为什么选 MySQL 不选 Redis？
- [ ] 🆕 能不能解释多轮对话的双重截断机制（Java 端 + Python 端）？
- [ ] 🆕 能不能讲清楚可观测性追踪了哪些指标、为什么用 SQLite 而不是 Prometheus？
- [ ] 🆕 能不能解释 token 估算的原理（中文字符 → token 的换算）和 ±15% 的误差意味着什么？
- [ ] 🆕 能不能讲清楚 Docker Compose 的网络模型（bridge + 容器名 DNS）和启动顺序控制（healthcheck + depends_on）？
- [ ] 🆕 能不能解释 Docker Compose vs K8s 的选型逻辑和概念对应关系？
- [ ] 🆕 能不能说清 Agent 工具调用的四层可靠性保障（描述约束 → 空结果信号 → recursion_limit → 流式降级）？
- [ ] 🆕 能不能讲清楚多轮对话的双重截断 + 成对删除，以及为什么是 10 轮？
- [ ] 🆕 能不能讲清楚 RAG 三层架构——深度解析（`##`主块+`###`子块+自动标签）、混合检索（向量+BM25→RRF融合→重排序）、防幻觉双检（来源强制标注+置信度分级）？
- [ ] 🆕 能不能用评测数据证明 Agent 效果——成功率 90%、平均 2.5 步、工具准确率 86.7%、知识/穿搭/对比意图 100%、影子测试 57% 结构化结果 vs 裸 LLM 0%？
- [ ] 🆕 能不能讲清楚 Redis 的两个落地场景（Python 对话记忆 + Java Session 共享）和降级策略（Redis 挂掉自动回退内存 dict）？
- [ ] 🆕 能不能解释高并发分层设计——工具闭包隔离 → 向量缓存 double-check → Redis 会话共享 → 三层降级不连锁？
- [ ] 能不能诚实说出三个不足 + 每个的改进方向？
- [ ] 写的最有技术含量的代码是哪段？（建议讲 Java SSE 透传 或 Agent 5 工具设计 或 trace.py 全链路埋点）
- [ ] 最大的坑是什么？怎么解决的？（建议讲 RestTemplate 缓冲了 SSE 流 或 Python 内存字典导致重启丢记忆）
- [ ] 这个项目让你学到最重要的是什么？（建议讲"理解了 Agent 是把决策权从代码转移给 LLM" 或 "先做可观测性再做功能——没数据就没法决策"）
- [ ] 有没有想过"LLM 本来就有知识，为什么还要语义检索 + 知识库"？
