# RAG 知识库模块 — 面试拷打应答

> 面试官如果对你简历上的"Agentic RAG"感兴趣，大概率从下面角度追问。
> 回答策略：先说结论，再说细节，最后补 trade-off。展现你知道的不只是"怎么做"，更是"为什么这样做"。

---

## Q1: 你这个知识库和商品搜索是什么关系？是不是多此一举？

**面试官在考**：你理不理解"知识检索"和"商品检索"的本质区别。

> 它们是两套独立的检索目标，服务于 Agent 的两个不同需求：
>
> - 商品搜索（search_products）回答"买哪个"——从商品库中找具体鞋款
> - 知识库（search_knowledge）回答"为什么"——从专业知识文档中找选鞋依据
>
> 举个例子：用户说"我扁平足，想买跑鞋"。如果没有知识库，Agent 直接搜"扁平足跑鞋"，找到一个关键词匹配的鞋就推荐——但 Agent 不知道为什么这双鞋适合扁平足，只能说"这双不错"。
>
> 有了知识库后，Agent 先查到"扁平足需要稳定型跑鞋（不能买缓震型），需要内侧支撑结构，ASICS DUOMAX 和 Brooks GuideRails 是典型技术"，再用这些专业条件去搜商品——推荐的不再是"我觉得好"，而是"因为你扁平足的内旋特征，你需要这些技术特性，这几双恰好有"。
>
> 两者的关系是：知识库告诉 Agent **选鞋的标准**，商品搜索告诉 Agent **库里有什么**。两个工具由 Agent 自主编排调用顺序。分开设计而不是混在一起，是因为它们的更新频率和使用方式完全不同——商品库随库存变化、知识库几周才改一次。

**加分话术**：面试官如果继续追问，可以补充——"这其实就是 Agentic RAG 和传统 RAG 的区别：传统 RAG 是检索→生成的固定流程，Agentic RAG 让 LLM 自主决定'什么时候检索''检索什么''检索结果够不够，要不要换个角度再检索'。"

---

## Q2: 知识库的内容是你自己写的还是从哪爬的？质量怎么保证？

**面试官在考**：你对待数据的认真程度，和对 AI 幻觉的态度。

> 知识库的 6 个 markdown 文件是我自己整理的——不是从网上随便抓取的。
>
> 信息来源：运动品牌官方产品页面（ASICS、Nike 官网的科技说明）、运动医学基础知识、跑步社区的公开评测和经验总结。我做了交叉验证，确保像"扁平足应该选稳定型跑鞋"这种医学相关的建议是准确且被行业公认的。
>
> 为什么自己写而不是爬一堆网页用 RAG？两个原因：
> 1. 数量匹配规模——我有 6 个主题，总共约 15000 字，靠人力整理不费力。如果知识库有几万篇文档才需要自动化
> 2. 质量优先于数量——爬取的内容噪音大（广告、过时信息、矛盾观点），LLM 检索到低质量内容反而会拉低回答质量。我自己写能确保每条知识都是准确的、结构一致的、和商品库相关的
>
> 更新策略：目前是文件 + git 管理。如果以后知识库维护成本变大，会考虑用 AI 辅助生成初稿 → 人工审核 → 入库的半自动化流水线。

---

## Q3: 为什么按 ## 标题分块？这个分块策略有什么 trade-off？

**面试官在考**：你对 RAG 中 chunking 策略的理解程度。

> 分块策略直接影响检索质量。我选择了按 `##` 二级标题分块，而不是固定字数分块。
>
> 为什么不用固定字数（比如每 300 字切一个块）？因为这会把一个独立的知识点切成两半。比如"扁平足的选鞋要点是选择稳定型跑鞋……绝对不能买 Nike Free 系列"如果刚好在 300 字断点处被切开，检索结果可能只返回下半段，Agent 看到"绝对不能买 Nike Free 系列"却不理解上下文，推荐时可能忽略这个约束。
>
> 为什么不用句子级别分块？太碎了——一个知识点需要上下文才能完整理解。比如"鞋底内侧需要有硬质支撑结构"这句话脱离了"扁平足"的上下文就失去意义了。
>
> 按标题分块的取舍：
> 优点：每个块是一个完整的知识点，语义自包含，LLM 直接可用
> 缺点：块的大小不均（有的 200 字，有的 500 字），个别大块可能超过 embedding 模型的最佳输入长度
>
> text2vec-base-chinese 支持 512 token 的输入，我的最大块约 500 字（中文约等于 500 token），刚好在边界内。如果以后加长篇知识文章，我会做二级分块——先按标题切，再对大块按段落做子分块。

**加分追问自问**：

> 面试官："你还知道哪些分块策略？"
>
> 你："递归字符分割（RecursiveCharacterTextSplitter）——按段落→句子→词语的优先级逐级切，保证不切到不完整句子。滑动窗口分块——块之间有 overlap，减少边界信息丢失。语义分块——用 embedding 相似度判断文本的'话题转换点'，话题切换时自动切。我项目目前按标题分够用，但如果是无结构纯文本（比如论文、合同），会用递归分割。"

---

## Q4: 为什么和商品检索共用一个 embedding 模型？会不会两个任务打架？

**面试官在考**：你对 embedding 模型的适用性有没有思考。

> 用的是 text2vec-base-chinese，一个为中文语义相似度优化的通用模型。
>
> 它不做商品专用向量、也不做医学专用向量，但在"通用中文语义匹配"上表现出色——能够把"扁平足""膝盖疼""减震"这些不同表达映射到相近的向量空间。这恰好是我需要的：用户输入的可能和知识库里的表述不完全一致，但语义相近。
>
> 两个检索任务共用这个模型是合理的：无论是查商品（"缓震好的跑鞋" ↔ 商品描述里的"减震跑鞋"）还是查知识（"扁平足适合什么鞋" ↔ 知识文档里的"扁平足选鞋指南"），本质上都是中文语义匹配问题。模型不需要知道检索对象是商品还是知识，它只需要判断文本相似度。
>
> 如果换专用模型会怎么样？用商品专用 embedding 模型——搜索商品的精度可能提升 3-5%，但知识库的语义匹配会变差（商品模型没训过专业知识文本）。反过来，用医学专用模型则商品匹配变差。通用模型在两个任务上的平均表现 > 牺牲其中一个去优化另一个。这是一个典型的"够用就好"场景——在 1000 个商品和 50 个知识块的数据规模下，模型差异远小于数据规模的限制。
>
> 额外好处：只加载一个模型，内存节省 ~400MB，启动快 ~3 秒。

---

## Q5: 知识库怎么更新的？如果商品库加了一个新品牌，知识库也要手动改吗？

**面试官在考**：你有没有考虑过系统维护性和数据一致性。

> 当前方案：知识库是静态 markdown 文件，通过 git 做版本管理。修改知识 → 改文件 → 重启 Python 服务 → 生效。
>
> 这确实是一个 trade-off——简单但需要手动维护。我选择这个方案是因为更新频率极低（几周一次），不值得做自动化。
>
> 如果以后要优化，改进路径：
> 1. 热重载：文件改动时自动重新加载（watchdog 库监控 knowledge/ 目录）
> 2. 增量更新：改了一个文件只重向量化那个文件，不重建整个知识库
> 3. 知识库版本管理：和商品库数据联动——比如商品库新增品牌时，触发知识库补充该品牌的介绍
>
> 但目前来说，6 个文件的手动维护成本远低于自动化系统的开发成本。我的原则是：工具链的复杂度不超过问题的复杂度。

---

## Q6: 你的知识库如果检索不到相关内容，Agent 怎么处理？

**面试官在考**：你的降级策略和边界 case 处理。

> 三层兜底：
>
> 第一层，Agent 的判断：如果 Agent 认为用户的问题不涉及专业知识（比如"帮我找一双 500 块以内的白色跑鞋"），它不会调 search_knowledge，直接搜商品。只有用户问了明显的专业知识类问题（"扁平足""EVA和Boost""怎么保养"），Agent 才会调知识库。
>
> 第二层，工具返回值：如果 search_knowledge 检索不到相关内容（相似度太低或知识库为空），返回 `{"message": "未找到相关知识"}`，Agent 看到这个结果后不会凭空编造专业知识——它会直接基于商品结果给出推荐，只是少了专业依据。这比返回不相关内容导致 Agent 幻觉要好得多。
>
> 第三层，启动降级：如果 knowledge/ 目录不存在或 6 个文件全部缺失，KnowledgeBase 返回零 chunk，embeddings 为 None。此时 `search_knowledge` 工具根本不会被注入 Agent（tools.py 里的 `if knowledge_base is not None` 判断）。Agent 完全退化为只有商品搜索的版本——不影响用户体验，只是回答少了专业深度。
>
> 三层兜底的核心理念：知识库是"加分项"而非"必选项"。它挂了，推荐功能正常运作——只是推荐理由从"有专业依据"降级为"有经验直觉"。

---

## Q7: 为什么不直接用 RAG 做所有事情？为什么要加 Agent？

**面试官在考**：你理解 Agentic RAG 和 Naive RAG 的区别。

> Naive RAG 的流程是固定的：用户提问 → 检索 → 拼接上下文 → LLM 生成回答。这种模型下，检索只发生一次，而且是"无脑检索"——不管用户问什么，都先检索一下。
>
> 我这里不需要，因为用户的问题类型多样：
> - "帮我找一双跑鞋" → 只需要搜商品，不需要查知识库
> - "扁平足怎么选鞋" → 需要查知识库，但不需要搜商品
> - "我扁平足 800 预算想买跑鞋" → 需要先查知识库（确定选鞋标准），再搜商品（按标准筛选）
>
> Agent 能自主判断"这次对话需要调哪些工具、按什么顺序调"。而且 Agent 可以做多轮——搜了一次商品结果不够，换个关键词再搜；查了知识库发现用户可能是高足弓（纠正了自己的判断），重新调整搜索条件。
>
> 如果用 Pipeline RAG（固定流程），以上灵活性全部丧失。Agent 的本质是把"工具调用的决策权"从代码逻辑转移给 LLM——代码只定义工具有哪些，LLM 决定怎么用。

---

## Q8: 知识库里如果有个错误，AI 会一直输出错误信息吗？你怎么防止？

**面试官在考**：你对待 AI 幻觉的态度和控制手段。

> 这个问题的本质是：知识库有没有错误的可能？有的话，LLM 会如何放大这些错误？
>
> 我的防护手段：
> 1. 知识库内容经过人工交叉验证——不是单源信息。比如"扁平足不能穿缓震型跑鞋"这个结论，我在 ASICS 官网、运动医学资料、跑步社区都看到了共识
> 2. 知识块的来源信息（文件名 + 标题）传递给 Agent——Agent 回复时可以引用"根据足型选购指南"，用户能溯源
> 3. system prompt 约束——`禁止使用 markdown 标题、不要写长篇大论、不要过度推销`——间接限制了 Agent 过度发挥的空间。Agent 被要求引用知识而不是复述知识，降低了逐字复读错误内容的风险
>
> 但目前缺少的一个关键防护：用另一个 LLM 做"知识一致性检查"。比如每天自动跑一轮测试：「输入"扁平足应该买什么跑鞋"，检查输出是否建议了缓震型——如果是，则标记为错误」。这个属于评测体系的一部分，还没做。
>
> 实话实说：在小数据量、人力维护知识库的阶段，内容错误的风险是可控的。规模化之后才需要自动质检流程。

---

## Q9: 既然用了 LLM，它本来就有知识啊，为什么还要知识库？

**面试官在考**：你对 LLM 的局限性的认知。

> DeepSeek 确实有通用知识——但它是从互联网语料学的，不是从鞋类专家的标准指南学的。区别在于：
>
> 1. 专业深度：LLM 知道"扁平足需要支撑"，但说不清楚"为什么缓震型会加剧内旋"、"DUOMAX 和 GuideRails 的技术区别是什么"。知识库提供了这种专业级别的细节。
>
> 2. 时效性：LLM 的 knowledge cutoff 是一个时间点，如果某品牌出了新技术，它不知道。知识库可以随时更新，确保推荐依据是最新的。
>
> 3. 幻觉控制：LLM 被问到专业问题时，可能会"自信地胡说"——把两个品牌的技术搞混、给一个不存在的人推荐不存在的型号。知识库约束了 LLM 的发挥空间——它只能说知识库里有的内容，不能凭空编造。
>
> 4. 品牌偏见：LLM 训练数据中某些品牌的内容更多（Nike 的互联网语料量 >>> Saucony 的），如果没有约束，它会不自觉偏向高频品牌。知识库给所有品牌均衡的覆盖，抵消了训练数据的偏差。
>
> 总结：LLM 的通用知识像是一个"什么都懂一点但什么都不精"的商品推荐；知识库让它变成了"有专业培训、有标准话术"的商品推荐。

---

## Q10: 你现在是文本检索。以后会考虑用多模态吗？比如用户拍一张鞋底磨损的照片来判断足型？

**面试官在考**：你的未来演进思路，看你是不是只会照着当前方案讲。

> 会考虑，但分阶段：
>
> 短期（当前）：文本检索已经覆盖了 80% 的用户需求——用户用自然语言描述问题，Agent 通过文本匹配找到知识和商品。这个阶段先把文本链路打磨好：知识库内容质量、检索精度、Agent 工具编排。
>
> 中期：多模态输入——用户上传脚型照片或鞋底磨损照片 → 视觉模型（GPT-4V 或本地 CLIP）分析足弓高度或磨损模式 → 从照片中提取结构化特征（足弓类型、内旋程度）→ 作为 Agent 的搜索条件。这个对提升推荐精度会有质的飞跃——用户不用知道自己是扁平足，照片就暴露了。
>
> 长期：如果要做的话，可能会用 OCR 识别鞋盒标签、商品吊牌等场景。但目前多模态的 ROI 取决于用户规模——如果 90% 的用户只是打字描述需求，花两周做多模态不值得。先用文本把核心体验做好，多模态是锦上添花。

**这道题的重点不在于多模态技术本身**，而在于你能判断"什么时候该做什么"——这是面试官最想看到的工程判断力。

---

## 快速自查（面试前一天过一遍）

- [ ] 能不能一句话说清楚"商品搜索 vs 知识库检索"的区别？
- [ ] 能不能解释为什么按 ## 标题分块而不是固定字数？
- [ ] 能不能说出知识库加载失败时的三层降级措施？
- [ ] 能不能对比 Agentic RAG 和 Naive RAG？
- [ ] 能不能诚实说出当前方案的不足和改进方向？
- [ ] 有没有想过"LLM 本来就有知识，为什么还要 RAG"这个问题？
